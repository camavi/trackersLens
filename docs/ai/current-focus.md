# Current Focus

Purpose: active work and immediate next step.
Read when: always after `AI.md`.
Do not read when: never during development sessions.
Last updated: 2026-09-18.

## Active Area

Managed Python graph relations: GLiNER2/NLI and the configured LLM are graph producers; TL persists their usable output with transparent evidence, provenance and quality labels instead of acting as a semantic rejection gate.

## Next Task

Unified Flow Map Chat provider integration (TASK-032) is the immediate implementation requested by the user. A single `AI Chat` icon in the persistent desktop sidebar opens the same chat from every app route; no duplicate Flow Map canvas trigger exists. The chat is now being evolved into a provider-neutral Agent Workspace: the user selects `Locale`, `Codex`, `Claude` or a future provider/model, and the selected AI decides when it needs Trackers Lens information. TL exposes declared, MCP-ready tools and returns their real structured results with provenance, evidence and limitations; it must not classify a user prompt or answer in the AI's place. Tool data and scopes require explicit consent. A provider may only propose a mutation; registered-tool validation, preflight, explicit confirmation and Time Travel remain TL-owned.

The hierarchical capability map QA is closed: the compact `tl.catalog.*` discovery tools expose TL domains/pages and domain-level tool indexes only. The provider must request a domain index, then a workspace/page/node tool, then explicitly authorized data. Node configuration questions now proceed from `inspectNode` key discovery to `inspectNodeConfig` value reads for only the fields required by the question; the consent dialog names the requested fields. A provider can propose one change to an existing text configuration field only after that exact field was read in the same turn; TL revalidates the node, shows old/new values, captures Time Travel and invokes the registered Safe Executor only after confirmation. The Runtime domain now supports workspace-scoped run indexes, one exact trace by ID, scoped logs and dry-run traces with the same tool chain and consent. Knowledge uses a node-scoped chain (`findNodes` → `inspectNode` → `inspectConnectedTools` → exact declared `tl.node.*` read); guessed node tool names are rejected. Providers exposes a consented safe-status read (installation/authentication/model/reasoning metadata only), never credentials or local paths. Python Runtime exposes a consented managed catalog read for pack/environment/model readiness and metadata plus a node-requirement resolver: after same-turn `findNodes`, it compares only the resolved node's declarative Python requirement with Core-managed pack state. That resolution gates a read-only managed environment/model status or, for a supported missing requirement, a safe Core install plan. A provider can then propose only the resolved pack installation; TL derives the trusted pack itself, requires confirmation and immediately displays a persistent English installation state, phase/progress and a keep-open notice. All Python projections exclude paths, filesystem handles, shell commands and lifecycle controls. No document, chunk, graph, runtime payload or node manifest may be transferred merely by discovering a capability.

## Current Work

- JSswift migration: QA closed. `jsswift` is the local UI dependency; desktop, workspace, popup and legacy options entries load its bundled CSS/JS, application code uses `window.JSswift`, and packaged fonts/icons resolve from its distribution. `postinstall` patches each distributed JSswift CSS asset to reference the local Material Symbols font rather than Google's URL, preserving the Electron CSP `font-src 'self' data:`. Electron smoke now loads Flow Map, Settings and Database Explorer through the real preload and asserts the JSswift runtime is present. Old framework assets remain untouched pending a separate deletion decision.

- AI Runtime Center SQLite projection: interactive QA passed. Opening the Center reads a Core-owned metadata projection for providers, agents, jobs, logs, memory and prompts instead of the previous 13 full stores. Jobs, logs and memory use explicit 25-record pages with `Carica altri`; full detail JSON is read only when opened. Editing a Provider, Agent or Prompt fetches its exact Core record first and merges form changes into it, preserving unrendered configuration; prompt writes retain their original AI prompt store.

- Statistics SQLite projection: interactive QA passed. Statistics requests one Core-owned aggregate containing only KPI counters, chart buckets and visible tracker/activity/endpoint/workspace rows. The renderer no longer receives whole widget, workspace, connection, performance, event or flow-log records. Refresh is explicit rather than a five-second background Core query, so the Statistics route cannot periodically interrupt desktop interaction. Full records remain available through their explicit Library, Connections, Flow Map, DevTools and Database Explorer surfaces.

- DevTools SQLite projection: interactive QA passed. Overview no longer loads the AI runtime data; opening AI fetches only a Core-owned provider/count/memory-summary projection, while full memory records remain explicit actions. The embedded DevTools panel owns vertical scrolling so long AI and Time Travel sections remain inspectable.

- Flow Chat fast-path performance: initial metadata now carries static descriptors for the common Flow, runtime, provider and managed-Python read paths. A unique Flow node can be resolved/inspected in one consented tool call and its selected values in the next; the Python path can then resolve the declared pack requirement directly. Dynamic node tools and future domains retain hierarchical discovery. Full envelopes stay inspectable in TL while only the newest observation and compact protocol state are carried into the next stateless provider call. Tool activity and the final external reply display the end-to-end provider time.
- Flow Chat provider account controls: Chat Settings now refreshes the selected Codex/Claude status, shows the authenticated account email only when the provider's official status command exposes it, and provides explicit Login, Logout and Refresh controls. The bridge never reads token stores, authentication files, credentials or raw status output; a connected provider that does not expose an email is reported transparently.
- Flow Chat external-provider QA gate: passed. User verification confirmed provider/account controls, two-read fast paths for node/Python questions with visible end-to-end timing, and the consented text-configuration update through the Safe Executor plus Time Travel. Next gate: Phase 5, applying the same provider-led tool/consent/action contract to Locale.
- Flow Chat Locale contract migration: QA closed. The Locale provider returned a typed `proposed_plan`; TL normalized it, ran preflight and appended the normal confirmed plan card. The misleading `Local fallback` badge was a presentation default for the new `provider-protocol` planner mode, not a legacy execution; it now displays `Provider proposal · Locale`. The unreachable universal classifier/planner, AI Settings self-repair and local-plan fallback have now been removed, so all new Flow plans come exclusively from the selected provider's typed contract. Palette normalization, preflight, Safe Executor, confirmed creation and Time Travel remain mandatory TL protections. Locale routing uses explicit creation verbs with word boundaries, so node labels such as `Embedding Generator` cannot accidentally request a Flow. Python-status questions retain one canonical fast path for every provider: `resolveNode` → `inspectNodeRuntime`.
- Flow Chat provider/Locale QA closure: approved. The user closed QA for all currently implemented provider, capability-map, consent, Locale creation and legacy-planner-retirement paths. The remaining Phase 4/6 entries describe future scope, not an open QA blocker for the implemented contract.
- Global external-provider account surface: QA closed. AI Runtime’s `AI Models & Providers` now always exposes Codex and Claude as global CLI-owned providers, including their safe installation/authentication/model status and global Refresh/Login/Logout controls. Chat Settings and AI Runtime now render the same shared account panel. Codex model, reasoning effort and speed are persisted as one global TL default in `tl_ai_providers`; every Codex chat resolves those defaults when sending, rather than retaining a second per-chat configuration. The controls call the same restricted Core bridge, so an account change applies everywhere; TL never stores, reads or copies credentials. Provider configuration deletion is disabled for these global account rows.
- Flow Chat target-scope safety: implemented and interactively verified. When Chat is opened outside the Flow Map route (for example Library), `Create flow` no longer uses `ensureRuntimeWorkspaceScope()` to fall back to the first persisted Flow. Its JSswift target dialog uses the same persisted Flow Map page/flow contract as the Library, excluding technical Core records such as `Draft Runtime Flow`, then uses the selected workspace id for both the Time Travel snapshot and materialization. The user verified that the target picker matches the two Library Flow Maps. On the Flow Map route, the active explicit workspace filter remains the target.
- Startup runtime-refresh freeze: resolved and interactively verified. Runtime Worker refreshes use only runtime nodes and dependencies; Flow Map canvas and Agent Runtime use the graph projection. This removes the automatic full SQLite snapshot that included Time Travel and froze the renderer for about 25 seconds. Full snapshots remain explicit for capture, restore, replay and diff.
- Flow Map SQLite startup/history projections: implemented and interactively verified. Opening a Flow Map requests channels, flows, runtime nodes, dependencies and connections; Node Runtime, Logs and Metrics use a Core-owned ordered page read and expose `Carica elementi precedenti`, preserving complete history inspection without serializing all event/log JSON at once. Collapsed event/context details defer JSON formatting until explicitly opened. DevTools lists Time Travel snapshot metadata in pages and fetches a full snapshot only for an explicit operation.
- Flow Map interaction rendering: implemented and interactively verified. The HTML node layer follows pointer movement immediately, while edge canvas, edge labels and minimap are coalesced into one animation-frame update. Edge rendering uses node-index maps; node drag and minimap updates reuse resolved DOM elements. This preserves the hybrid HTML/canvas architecture while keeping pan and drag fluid on the verified multi-node Flow.
- **P0 — SQLite read performance across every page:** closed and interactively verified. Automatic page loads, refreshes and Live Test polling use Core-owned scoped projections, pages or aggregates instead of renderer-facing full-store reads. Database Explorer, Library/Flow Map Library, Workspace, Connections, Flow Map, DevTools, Statistics, AI Runtime Center, inspector actions, Flow Chat memory ranking and Live Test run records have all passed interactive QA. Complete SQLite records remain available only through explicit inspection, edit, export, deletion-preflight, capture, restore, replay or diff actions. Paging preserves complete inspection and export without hidden limits, truncation or data loss.
- **P0 — Live Test run polling:** interactively verified. The Flow Map Live Test requests only `tl_events`, `tl_flow_logs`, `tl_ai_jobs` and `tl_ai_logs` matching its active workspace/run (and agent while awaiting a node job) through a Core-owned SQLite query. It no longer calls complete Event, Flow Log or AI Runtime stores every polling cycle. The run scope remains complete and inspectable; no record cap or truncation was introduced. The user verified it remains responsive while coordinating LLM and Python runs.
- Flow Chat declarative tool coordinator: interactively verified. Every selected provider receives the same per-turn protocol state: completed read identities, resolved node/requirement counts, and a small set of prerequisite-valid next actions derived from the declared tool graph. It does not decide a question's answer or execute a suggested read; it prevents providers from reconstructing already-known state and makes repeated reads explicit.
- Python runtime evidence summary: interactively verified. `inspectNodeRuntime` projects the objective `requirementStatus`, `environmentStatus`, `runtimeStatus` and `modelStatus` separately, with an explicit reporting rule. Codex and Locale both used the shared two-read path and reported a stopped runtime separately from ready requirement/environment/model states.
- Persistent desktop navigation (TASK-031): `app.html` is the sole desktop shell and route authority. Library, Library Flow Map, Workspace, Settings, AI, Runtime Python, Statistics, Connections, DevTools, Profile and Database Explorer are embedded views with a persistent sidebar/outlet and explicit cleanup. Internal route state is canonicalized as `app.html?tl-route=<view>` while preserving ordinary query/hash state, so refreshes no longer depend on legacy per-page documents. The twelve retired desktop entry pages and their redirect helper have been removed; popup/recovery links now target the canonical shell. Flow Map has an explicit bootstrap lifecycle and is registered in the shell.
- Persistent desktop navigation lifecycle QA: approved. The user closed QA for the current Flow Map lifecycle; the remaining legacy-route cleanup is future migration work, not an active QA gate.
- Python runtime direction is being clarified in `docs/ai/python_migration_guide.md`: TL will integrate established Python modules through thin capability adapters rather than recreate specialist algorithms. Future Python node manifests must declare managed module/pack requirements, pinned versions or lockfile and install policy; Runtime Manager resolves them only through approved environments with explicit install consent when needed.
- Python node audit is complete for the 88-node Flow Map palette in `docs/ai/runtime/python-node-audit.md`. No control-plane node is a Python migration target; `text.embedding` for Embedding Generator/Vector Memory is implemented, and `text.rerank` is now implemented for RAG Search only. Graph Query remains a later separate decision.
- `tl-node-execution/v1` now normalizes declarative `dependencies.python` requirements (`environment`, modules/version constraints, lockfile and install policy) without triggering installation. The `text.embedding` managed-resolver/pack path is the next implementation slice; the existing provider/local-hash embedding path remains unchanged.
- The Core-side Python pack resolver now matches declared requirements only against trusted (`built-in`/`verified`) ready packs and otherwise returns a blocked/unavailable state plus a consent-requiring install plan. It exposes no installer. Next is wiring a feature-gated, pinned `text.embedding` pack to the production execution path.
- NLP managed pack: `runtimes/python/packs/nlp/` pins Sentence Transformers 5.5.0 and a revision-pinned Apache-2.0 multilingual local model. Embedding Generator, Vector Memory and RAG Search declare the pack in `dependencies.python`; their configuration shows its readiness and routes a missing pack to Runtime Python e Modelli. The Core-owned installer shows the trusted pack, lockfile, pinned requirements, model revision and network effect, then requires explicit confirmation before creating/reusing the managed environment, installing the lockfile and downloading the declared model. Selecting `python-local` locks embedding dimensions at the model’s fixed `384`; no Node, renderer or worker can invoke pip or download independently.
- **Runtime Python e Modelli** is a dedicated top-level page, linked by its own global sidebar icon—not a Settings subsection—so the inventory can scale to many packs and models. Its Core-owned transparent catalog reports declared pack versions/dependencies, managed environments and installed local models with exact on-disk size, revision, dimensions, languages and license. The renderer receives no local filesystem paths or shell capability. Model removal accepts only a registered opaque model ID, requires a CMS confirmation and stops the relevant managed runtime before Core deletes that exact model directory. Trusted pack installation is implemented as a separate explicit-consent lifecycle, with actual Core-published progress.
- Python pack install UX: creating a Node whose default capability requires a missing Python pack opens a non-blocking install prompt. “Install” opens Runtime Python e Modelli with the exact pack plan; “Non ora” preserves the node but it fails explicitly at execution until the pack exists. The Core installer publishes real lifecycle progress (prepare, environment, locked dependencies, version verification, model download, runtime start) to the UI. Python-local embeddings no longer silently fall back to local-hash when the pack is absent.
- Python default-requirement prompt fix: `requiredByDefault` is now preserved by the normalized execution dependency contract. This keeps the insertion dialog generic: Dictionary Builder prompts for its annotations pack just as RAG prompts for its pack, without hard-coding Node subtype names.
- Dictionary Hybrid cutover: `hybrid` is now the explicit default. spaCy creates local linguistic proposals, then the configured LLM sees only those proposals plus their source chunks and may accept/reject them; it cannot add an independent dictionary term. TL validates returned JSON, proposal membership and exact evidence before persistence. A missing/invalid verifier fails Hybrid explicitly rather than silently saving unverified spaCy output. `python-spacy` remains the local/fast opt-in mode; `rules` adds declarative TL Custom Rules; `llm` remains an explicit model-guided mode. Custom Rules never modify spaCy annotations.
- Dictionary Hybrid provider UX: missing provider configuration, unavailable model and unreachable local provider are converted into actionable runtime errors. Knowledge Runtime emits a safe browser runtime-error event and Flow Map displays it as a visible alert on the active canvas; technical fetch stacks remain diagnostic-only.
- Dictionary Hybrid emit regression: a stale reference to the retired fallback counters (`minHybridAiTerms` / `useRuleFallback`) could throw only after a successful LLM verification, preventing the `knowledge.dictionary.updated` event from reaching downstream Preview nodes. Hybrid now reports its direct verified state without legacy fallback metadata.
- Dictionary Hybrid prompt-mode regression: the retained `micro` validation branch now declares its prompt-mode flag locally. This prevents a successful full Hybrid provider response from failing after validation with `micro is not defined`.
- Flow Map runtime-error release: a Knowledge Runtime error carrying the active Live Test `runId` now finishes that test immediately, clears active nodes/edges and re-enables Node Play controls. Background errors without a matching test run do not alter test state.
- Flow Map node menu interaction: selecting or moving a Node no longer opens the left inspector automatically. Each Node header exposes an explicit toggle menu button immediately beside Settings; it opens/closes the inspector for that Node on demand while normal canvas selection remains unobtrusive.
- Dictionary spaCy quality follow-up: a user fixture exposed sentence-initial verbs/adjectives mislabelled as `PROPN`. TL now rejects sentence-initial generic spaCy entity/PROPN proposals unless a declared-name context supports them; spaCy `PER` proposals remain distinct as `spacy-person`. This is generic evidence validation, not a document-specific vocabulary filter.
- Dictionary spaCy PER validation follow-up: the fixture showed that the Italian small pipeline can also label a finite verb such as `Attesero` as `PER`. TL now cross-checks every spaCy entity against its covered POS tokens and rejects entity spans containing `VERB`, `AUX` or `ADJ`; genuine person names remain accepted without a story-specific exception.
- Dictionary multilingual proper-noun validation: a German fixture showed that capitalized common nouns—and even false spaCy location spans—could be accepted by the LLM as `proper-noun` and incorrectly become core seeds. TL now requires an exact named-entity span (`PER`/`PERSON`/`ORG`/place labels) plus a covered `PROPN`/`NE` token, or a declared acronym, before preserving that type; unsupported proposals become ordinary `term` entries, without changing the source text or adding document-specific rules.
- Knowledge/Python Runtime visibility: the existing persisted Node Runtime timeline is now exposed for every Knowledge Node, not only LLM-oriented cards. Managed Python calls (spaCy annotations, Sentence Transformers embeddings, Hybrid RAG retrieval and CrossEncoder reranking) add durable start/result/error steps with safe input/output summaries and duration. An open `View Runtime` timeline refreshes live and terminal writes wait for any active live write, so `Working` cannot overwrite the final `Complete`/`Error` state.
- Knowledge/Python Runtime attribution: Flow Map listeners can process one event concurrently. Each managed Python call now receives its exact Node runtime context, so the Dictionary timeline records only its spaCy activity while `text_embedding` appears only in the Embedding Generator/Vector Memory timeline that invoked it.
- Knowledge Graph Builder hybrid relation foundation: the Builder declares the managed `trackerslens.graph.relations.gliner2` requirement and defaults to `GLiNER2 local + multilingual NLI + LLM only for ambiguous cases`. The local worker receives only TL-authorized chunks and a TL-controlled relation schema. GLiNER2 returns sentence-grounded candidates; the managed local `MoritzLaurer/mDeBERTa-v3-base-mnli-xnli` verifier scores entailment/neutral/contradiction from each exact quote; the configured LLM receives only the remaining ambiguous candidates. TL organizes scope, persistence, de-duplication, provenance and quality labels. The isolated `graph` environment, pinned GLiNER2/NLI models and protobuf runtime are installed and verified; controlled installation/inference and multilingual QA are complete.
- Knowledge Graph Builder quantity audit: an end-to-end run produced 33 GLiNER2 candidates and 21 NLI acceptances, yet only 12 relations reached SQLite because a second legacy TL narrative-compatibility heuristic rejected 9 already NLI-verified relations. The duplicate heuristic no longer applies to `gliner2+nli` output. TL still enforces scope, exact source quote, valid endpoint records, configured schema and threshold; the heuristic remains for unverified LLM/rule proposals. This removes an implementation bottleneck rather than adding book/language-specific filtering.
- Knowledge Graph grounded-recall correction: GLiNER2 sentence-plus-chunk extraction is intentionally high recall, but its endpoints are now materialized only when both resolve through the current document's core/typed Dictionary seeds or an entity produced by another non-Builder node. The canonical registry supplies the real entity type and aliases; prior Builder output is excluded so a noisy run cannot authorise the next. This removes the structural German-uppercase failure (ordinary nouns becoming `proper-noun`) without introducing story or language filters. Runtime output exposes raw, grounded and rejected-ungrounded candidate counts.
- Knowledge Graph provenance follow-up: the audit showed that legacy local Entity Extractor capitalisation records could still enter the canonical registry and reintroduce false sentence starters such as `Sembrava` / `All'improvviso`. Bare capitalisation extraction now rejects sentence-initial tokens, while legacy `proper-noun` records require independent Dictionary confirmation before a Graph Builder run can use them. Dictionary type authority wins over lower-level extraction confidence. NLI-verified technical relations now additionally require technical context, and lexical guards remain active for `reveals`, `encounters` and `represents` where co-occurrence alone is insufficient.
- Knowledge Graph semantic-invariants follow-up: audit of the first Italian graph found NLI accepting a location relation from movement (`anziana -> lives_in -> Montagna`) and broad relation/type drift. The Builder now preserves `creature`, `role` and `quote` types declared by the Dictionary, and requires direct residency/search cues for NLI `lives_in`/`seeks` candidates. This is relation-semantic validation applied equally across languages and domains, not a narrative filter.
- Knowledge Graph evidence-registry recall follow-up: a full Italian run produced 132 GLiNER2 candidates but only 15 grounded endpoints because the Builder consulted only 31 core/typed Dictionary seeds despite 76 source-evidenced dictionary entries. The canonical registry now additionally admits recurring lexical entries (two or more source occurrences) as endpoint evidence while core/typed seeds retain precedence for labels and types. This restores generic recall without allowing bare capitalization or unsupported one-off fragments to become graph authority. Ordered Event Builder output remains context only until its own subject/object accuracy is independently improved.
- Knowledge Graph supervision simplification: a subsequent run fell to 7 entities / 1 semantic relation because Hybrid had become a chain of gates: TL called the LLM only for NLI-ambiguous candidates, instructed it not to discover other facts, then discarded every non-ambiguous LLM proposal. This has been removed. Hybrid now means one full LLM graph-construction/verification pass over source chunks, Dictionary registry and explicit Event context; Python GLiNER2/NLI contributes suggestions, while TL retains only scope, schema, canonical endpoint and exact-evidence validation. No migration compatibility behavior is retained for this pre-release system.
- Knowledge Graph organizer-only cutover: the remaining canonical-endpoint, exact-evidence, allowed-type, confidence and narrative-compatibility gates have been replaced by persisted `metadata.quality` (`verified` or `attention`) with concrete warning codes and producer provenance. GLiNER2 candidates reach NLI without a Dictionary admission gate; accepted, ambiguous and rejected NLI results are all retained. LLM relation endpoints materialize as graph entities when needed. TL rejects only an empty/non-representable endpoint, preserves the original relation label, and no longer injects its retired narrative rule-supplement facts. The legacy narrative/technical semantic-compatibility judge is deleted rather than repurposed as a noisy warning: quality labels now report only objective producer/evidence/schema/NLI conditions. Knowledge Graph’s Info/Selection UI exposes attention counts and entity warning labels so users can judge output without hidden deletion.
- Knowledge Graph provider-context recovery: a local model may expose a smaller real context window than the saved provider configuration. If its first full Graph Builder request receives a context-size `400`, TL reads the provider-reported `n_ctx` and retries with a transparent context-bound prompt: all source chunks are retained within a calculated per-chunk budget, while duplicate persisted graph/event/Python/Dictionary context is omitted only from that LLM retry. The runtime trace records the real context, prompt budget and omitted duplicate sections. TL data is never deleted or semantically filtered by this transport recovery.
- Knowledge LLM context recovery: Dictionary Builder and Entity Extractor now treat `full → compact → micro` as transport retries even in Hybrid mode. If a compact request succeeds, TL preserves that result and processes every remaining source chunk individually before merging it, so the provider window cannot become a document-level filter. Compact/micro requests truly reduce request text when no explicit node chunk budget exists. Semantic Relation Enricher transports every candidate in two-candidate batches and applies the same `full → compact → micro` request recovery: compact modes shorten only duplicate chunk context, never the candidate set. These are context-window recoveries, not semantic filters or data loss.
- Knowledge Graph relation recall/verification follow-up: a live graph audit showed that whole-chunk GLiNER2 extraction produced only 33 candidates across 8 chunks and some NLI decisions used a weak generic fallback hypothesis for relation types without a dedicated template. GLiNER2 now also extracts each source sentence and deduplicates evidence-backed candidates; NLI uses an explicit semantic proposition for every built-in graph relation type, while an unknown future type remains ambiguous for LLM review instead of being auto-accepted through a generic phrase. No document-specific vocabulary or relation cap is introduced.
- NLI verifier follow-up: the unsupported-template branch now initializes and retains its explicit `verified` result list before evaluating candidates. This prevents the managed worker from raising `name 'verified' is not defined`; the branch stays data-only and routes only an unknown relation type to LLM ambiguity review.
- Knowledge Runtime dialog live refresh: an open timeline refreshes immediately from its EventBus event and also performs a bounded 750 ms read-only state check. Rendering occurs only when the job signature changes, preserving the currently opened timeline details while covering event paths that bypass the Flow Map listener.
- Dictionary to Graph Builder integration: Knowledge Graph Builder Agent now accepts Dictionary outputs and reloads the same scoped `core`/`typed` `usableAsSeed` entries. It sends them to the configured LLM only as verified type/alias disambiguation hints; every proposed entity/relation still requires an exact quote from a scoped chunk. The resulting runtime output records the exact Dictionary seed ids/count used for transparent inspection.
- Graph Builder snapshot routing: `Knowledge Graph` now accepts `knowledge.graph.proposed` and `knowledge.graph.enriched` in addition to base entity/relation events. Connect Graph Builder's proposal output to it; the Graph Builder always emits that completion signal, while `graph.enriched` exists only when semantic relations were accepted. This guarantees a new `knowledge.graph.updated` snapshot for Preview/Graph Query. A Dictionary output must not be connected directly to Knowledge Graph: it contains lexical hints, not graph facts.
- Knowledge runtime schema evolution: saved Flow Maps retain their historical input arrays. Built-in Graph and Graph Builder channels are therefore normalized at runtime as well as in the palette, while the dependency/source ownership gate still blocks unlinked events. Existing canvases receive the new Graph Builder completion route without requiring a user migration or Node recreation.
- Knowledge connection-contract repair: early Flow Maps could persist stale target ports; one critical Entity Extractor → Semantic Relation Enricher link even used `knowledge.entity.created` instead of the Enricher's `knowledge.relation.created`. Flow Map now repairs and persists those canonical chunk→Dictionary, Dictionary→Event, Entity/Semantic/Event→Builder and Builder→Graph contracts on load, and exposes Entity Extractor's relation output in historical manifests. The fix is structural and applies to every document/language.
- Knowledge Graph delivery fix: Flow Map, the runtime-worker controller and the worker all now use the Graph-proposal routing revision through one immutable script token. A full app restart therefore creates a new worker and loads the updated Graph subscription instead of running a cached worker that imports an older Knowledge Runtime.
- Knowledge Graph Builder memory reconciliation: `replaceExisting` no longer deletes the Builder's prior document graph before GLiNER2/NLI/LLM can inspect it. The Builder now uses retained evidence-backed entities and relations as context, preserves exact relations reconfirmed by the current run, and removes only stale Builder-derived records after a successful replacement. This prevents duplicate growth without turning every Node Play into a cold-start graph reconstruction.
- Knowledge Event Builder to Graph Builder route: `knowledge.events.updated` and `knowledge.event.context` are now explicit Graph Builder inputs. A connected Event Builder sends ordered event payloads (sequence, participants, roles, modality, polarity and source quote) to the Builder; the Builder forwards them only as chronology/causality context to the ambiguous-case LLM and still requires every persisted entity/relation to quote a scoped chunk. `knowledge.graph.proposed` retains the exact event-context count/channel for downstream inspection. It never reads unrelated workspace events implicitly, so the visible Flow Map dependency remains the sole data route.
- Knowledge Event Builder Rules quality gate: Hybrid still preserves evidence-backed LLM events and may merge local coverage, but a Rules candidate for an actor-driven action (`drinks`, `moves`, `speaks`, `takes`, etc.) now requires an explicit or directly resolved coreference actor as well as its declared action cue. Unresolved context-window guesses are rejected with an inspectable reason before they can become graph chronology/causality context. This is generic validation, not a filter for a particular book or language.
- Knowledge event delivery safety net: after any Knowledge Node persists an output event, it forwards that exact event to directly connected Knowledge targets if a worker refresh temporarily left their EventBus subscription map stale. Normal EventBus delivery and the safety net share one execution key, so Dictionary → Graph Builder and Graph Builder → Graph work without duplicate executions.
- Knowledge token persistence: Runtime Graph Store now normalizes every renderer record to IPC-safe data before the Electron SQLite bridge. Token accounting therefore cannot fail because a transient UI callback/reference cannot be cloned.
- Knowledge token persistence fix: UI-only callbacks/references can be present transiently on a renderer Runtime Node. Before persisting AI token totals, Knowledge Runtime now serializes a data-only snapshot and refuses an invalid snapshot explicitly. This keeps token accounting from failing with IndexedDB's `object could not be cloned` error or destabilizing downstream runtime state.
- Python model download progress is retained in the installation dialog as well as the inventory page. The Core installer measures the actual temporary managed-model directory every 750 ms, so byte/percentage progress does not depend on inconsistent provider output. A Flow Map Node can complete the same trusted plan in an overlay dialog without navigating away from the canvas. Model manifests may declare a strict file allow-list: the RAG CrossEncoder downloads only the files required for local inference, never optional ONNX/OpenVINO artifacts from the same repository.
- A deliberate application close during a declared-model download no longer invalidates the exact Core-owned temporary directory. On the next explicit install action TL resumes only when Hugging Face's pinned-revision metadata is present; an unknown or unverifiable partial directory remains blocked and is never executed or silently trusted.
- Managed model downloads retry transient Hugging Face connection resets three times with one worker before reporting failure. If the provider still disconnects, TL reports a concise recoverable network error and never treats a missing or unverified partial directory as installed.
- During a model transfer the visible progress bar represents that model's measured byte percentage, exactly matching its byte label; overall pack lifecycle progress is not used as a visual substitute. The measurement counts only the declared final assets and the largest active partial file for each asset, never duplicate/stale Hugging Face cache fragments.
- Flow Map Live Test now synchronizes page-owned runtimes before emitting its root event. This guarantees Document Store -> Chunk Processor -> Embedding Generator subscriptions are present and prevents a stale background runtime owner from dropping a Knowledge pipeline test event.
- RAG Search regression fixed: the search fallback for an unspecified `topK` now waits until the chunk records have loaded, avoiding the temporal-dead-zone runtime error that previously stopped a valid manual RAG query before it could emit context.
- RAG uses the managed Python Hybrid pack in this first-development phase. `runtimes/python/packs/rag/` declares trusted, pinned `bm25s==0.3.11` alongside Sentence Transformers 5.5.0 in the existing local NLP environment. TL sends only scope-authorized query/candidate vectors and text to `rag.hybrid_search`; the Python adapter uses maintained Sentence Transformers semantic search plus BM25S lexical retrieval and returns separate semantic, lexical and fused scores. SQLite, scope, permissions, context construction and query persistence remain TL-owned. The JavaScript cosine ranker and Legacy RAG selector have been removed.
- `npm run test:rag` is the single repeatable RAG suite: pack contract plus generic Italian narrative and technical/informational fixtures. The fixtures check causal, procedural, object, storage, indexing, retrieval and permission questions only for source-supported Hybrid RAG Top-3 evidence. They are test-only and must never become runtime filters, expansions, boosts or story-specific rules.
- RAG Search is Python Hybrid-only in this first-development phase. The old JavaScript cosine ranker, legacy selector, migration UI and compatibility fallback have been removed. TL still owns scope, SQLite, permissions, context construction and persisted outputs; the managed Python pack owns semantic and lexical ranking.
- RAG Search now also requires its pinned local `text.rerank` capability: after Python Hybrid retrieval, the built-in `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1` CrossEncoder reorders only TL-authorized candidate text. The model revision is pinned in `trackerslens.rag.hybrid`; a missing reranker leaves this pack unavailable, prompts installation for a newly added RAG node and fails existing execution explicitly. There is no user migration or legacy-ranking mode in this first-development phase.
- Clean AI/Preview RAG output preserves retrieval provenance for transparent verification: `python-hybrid-rerank`, hybrid algorithm/weights, candidate counts, the pinned CrossEncoder model/revision, plus each source's Hybrid and rerank score. It exposes no Python path, model files or runtime handle.
- Preview JSON normalization: Preview keeps the original runtime payload untouched, but in `auto`/`json` display modes it now recognizes a complete Markdown-fenced ` ```json ` response, parses it and renders formatted JSON. Invalid JSON and `raw` mode remain verbatim, so model output is never silently repaired or overwritten.
- RAG end-to-end validation is complete: `Document -> Python embedding -> Python Hybrid RAG -> CrossEncoder rerank -> AI Debugger -> Preview` produced source-grounded positive and negative-case behavior. The emitted provenance confirms the pinned `cross-encoder/mmarco-mMiniLMv2-L12-H384-v1` ran after Hybrid retrieval and exposes per-source Hybrid/rerank scores. For an unstated cause, the generic AI prompt did not fabricate a causal fact; it explained the documented condition with evidence. Exact abstention wording remains a user-controlled system-prompt choice, not a story-specific runtime rule.

- Electron desktop persistence is SQLite-only by product decision. Graph, observations, pages/widgets, connections, channels, Knowledge, Workspace editor and StorageRuntime use TL Core SQLite.
- `database.html` is the desktop read-only SQLite Explorer: it lists Core-approved logical collections, their record counts and JSON records through the restricted preload bridge; no database path, handle, arbitrary SQL or demo-data fallback reaches the renderer. The UI exposes only inspection, copy and local JSON export—not mutation actions.
- Settings obtains desktop persistence status directly from `trackers.desktop.persistence.getStatus()` and contains no import, shadow, activation or recovery controls.
- Flow Map storage uses `Save SQLite Record` with `sqlite.write`; old persisted manifest tokens are normalized to SQLite permissions only when loaded.
- `StorageRuntime` requires the TL Core SQLite bridge and fails explicitly outside the desktop application.

- Connected Node Tool Protocol v1 is being defined so Agent/LLM nodes can inspect connected nodes, plan tool calls, request full or partial document evidence, ask Dictionary/Event/Graph/Memory nodes for focused observations, verify answers from source-bearing evidence and only then respond or emit validated actions.
- The protocol is being kept MCP-ready: TL node tools should map cleanly to MCP tools/resources, while TL remains the local runtime owner and all mutation still goes through safe executor/preflight.
- Phase 1 manifest work has started: Agent Runtime can inspect MCP-ready connected tool manifests, and Orchestrator graph neighborhoods now expose `agentTools` in node capabilities without executing tool calls.
- Phase 2 read-only executor now covers Document/Text/Memory plus Dictionary/Event/Graph-style nodes through `callConnectedNodeTool`.
- Inspector UI/debug now includes an `Agent Tools` panel for selected nodes, with connected tool manifests, read-only probe calls and the latest result envelope.
- Phase 3 planner base has started: Orchestrator AI planning can emit `call_tool` steps, execute read-only connected tools, observe `agent.tool.observation`, and feed those observations back into the next planning pass.
- AI Agent direct-answer base now gathers connected tool observations before the LLM prompt, so normal AI Agent answers can be grounded by connected Document/Dictionary/Event/Graph evidence without relying only on upstream fixed context.
- Agent tool reachability now follows the connected runtime chain/component instead of only direct neighbors, while disconnected workspace nodes remain blocked. The new `tool-access` link role gives Agents permission to interrogate connected nodes without turning that edge into normal event/data execution.
- Flow Map has an `Agent Tools Test` sample that creates a short data-preparation path (`Document Store -> Chunk Processor -> Dictionary/Event/Entity`) plus `tool-access` edges from one AI Agent to the readable Knowledge nodes, so the new architecture can be tested without rebuilding the old long fixed pipeline.
- AI model selection for Knowledge/Agent configuration now preserves the selected provider model instead of falling back to the `local-model` placeholder; the Agent Tools sample resolves the real local provider model before creating AI-backed nodes.
- Flow Map runtime execution now enforces a single owner: starting page runtimes stops the background worker for that workspace, and starting the worker stops page runtime subscriptions. This prevents duplicate Knowledge/AI provider calls from page+worker processing the same event.
- LM Studio Knowledge AI calls no longer send OpenAI `response_format: json_object` by default. JSON is still enforced by prompt/validation, but this avoids the automatic two-POST fallback when LM Studio rejects JSON mode.
- Knowledge Mechanism Cue Agent is being added as a first-class Knowledge LLM node: it exposes system prompt, prompt template and output instructions for mechanism/evidence cue generation, emits `knowledge.mechanism.cues`, and lets Graph Query consume validated external cues instead of relying only on its hidden fallback prompt.
- AI Agent/Knowledge cleanup is active: hidden story-specific query expansions, answer-shaping examples and sample-material ranking boosts are being removed from runtime paths. Direct AI Agent connected-tool planning now keeps generic tool calls and only retries a short LLM final-answer pass when LM Studio thinking models exhaust `max_tokens` with empty `message.content`.
- Agent connected-tool scope is direct by default: if an Agent is connected to only one readable node, it can inspect/call only that node's tools, not the whole downstream runtime component. Document Store tool calls now expose debug counts for scoped documents/chunks, selected chunks and returned character counts; `getFullDocument` reconstructs the source from ordered chunks when the stored document record is shorter than the chunk set, and AI Agent prompt rendering preserves the larger full-document evidence window instead of clipping it to the normal short evidence preview.
- Flow Map AI control ports now keep a single `agent_control` runtime contract but expose four visual handles on every node: top-left and bottom-left for IN, top-right and bottom-right for OUT. This makes tool/agent links easier to route without creating extra runtime ports.
- Flow Map AI Agent aliases are being corrected to behave as local override nodes: editing an alias must not update the source agent or sibling aliases; aliases inherit future source-agent changes only for fields that have not been locally overridden.
- AI Agent runtime visibility is starting: Agent jobs now carry step timelines and emit `ai.agent.step`, and Flow Map Agent nodes expose a `View Runtime` dialog for read-only run status, task, provider and step inspection.
- Agent node cards now surface the latest runtime step as a badge/footer state such as Working, Using tools, Calling model, Complete, Fallback or Needs user.
- AI Agent long-output continuation is now being added: when a provider stops with `finishReason: length`, the runtime can issue bounded continuation calls, merge the output and expose continuation steps in the runtime timeline.
- Continuation settings now use `Max Continuations` default 10; `0` means unlimited continuations for controlled long-running jobs, and the editor warns on unlimited or high values.
- Agent memory management follow-up: the AI Agent editor Memory tab now has a per-agent `Clear Memory` action that deletes stored memory for the current runtime agent without touching jobs, logs or configuration.
- Agent memory management is moving to a reusable dialog: the editor, Agent node body and Agent Runtime viewer can open a shared memory manager with record counts, per-record deletion and clear-all.
- Agent memory persistence follow-up: AI Agent responses can now be saved automatically as agent memory records when `Save Responses` is enabled and Memory Persistence is not `none`.
- Agent runtime input trace follow-up: the Runtime dialog is adding `Full Prompt` and `Full Trace` visibility so each run can show objective, payload, memory, input history, connected tools and the exact provider prompt.
- Agent memory policy follow-up: `Read Memory` is now separated from `Save Responses`, so an Agent can save outputs without injecting previous memory into the next prompt.
- Agent fresh-run follow-up: AI Agent nodes now expose `Fresh Run` as a toggle policy, so the next normal upstream-triggered execution ignores memory read and input history without auto-running the Agent or changing saved Agent memory settings.
- Agent trigger trace follow-up: AI Agent input traces now include trigger mode, source node, dependency/link id, ports and channel so unexpected starts can be audited from the Runtime dialog.
- Agent execution guard follow-up: AI Agents now expose `Trigger Policy` (`On connected event`, `On any accepted input`, `Manual only`) and the runtime enforces it before starting from bus events.
- Agent node policy summary follow-up: AI Agent cards now show trigger policy, Fresh Run state and memory read/save status directly on the node body.
- Agent alias diagnostics follow-up: AI Agent alias nodes now expose a read-only diagnostics dialog showing the source agent, alias node identity, local override paths and the resolved runtime/memory policy.
- Task node objective UX follow-up: long `Objective`/note payload fields now open a compact textarea dialog from the node card, keeping the canvas dense while allowing full prompt editing.
- Entity Extractor LLM QA follow-up: current traces show the weak graph is a mix of small-model JSON/schema drift, prompt ambiguity around relation endpoint labels and a runtime endpoint-resolution gap after cross-chunk entity dedup. The runtime now salvages malformed provider JSON, keeps raw provider payloads inspectable, rejects placeholder relation targets in the prompt and resolves LLM relation endpoints against the global run entity set when chunk-local entities were deduplicated.
- Structured/World Knowledge foundation started: `Structured Knowledge Store` persists typed schema/versioned JSON records in a generic local store, while `World Database` uses the same store with `worldbuilding/v1` records for worlds, kingdoms, packs, classes, personalities, names, story blocks and stories. World DB validates hierarchy/story-block references and emits `world.database.updated` with graph-shaped data inside the world payload, with semantic visual links only for explicit hierarchy/usage relations, without emitting `knowledge.graph.context` or mutating the standard Knowledge entity/relation graph. `World Graph View` is now the explicit visual node for `world.database.updated` maps, with search, zoom/pan controls, animated nodes and draggable manual node positions, keeping worldbuilding visualization separate from the standard Knowledge Graph. Flow Map inspector now has dedicated Structured/World debug export tabs.

## Completed Sequence

1. Step 1: runtime/DB query tools.
2. Step 2: dependency-aware commands.
3. Step 3: more realistic endpoint research.
4. Step 4: real compound commands.

## Latest Completed Work

Trackers Lens Agent Runtime v1 has started:

- `core/runtime/agent-runtime.js` adds the first Codex-like runtime control layer for agentic flows, exposed as `window.TrackerLensAgentRuntime`.
- v1 tools are English-first internally and include `inspectFlow`, `runFlow`, `inspectNode`, `readLogs`, `suggestFixes` and `listRuns`.
- The first `runFlow` implementation is intentionally trace-first/dry-run: it builds an ordered runtime plan from the real graph, records in-memory run traces, emits `agent.runtime.run.completed`, and does not bypass existing node runtimes or safe executor rules.
- Flow Map now exposes an `Agent Run` topbar dialog for Agent Runtime inspect, dry-run trace, safe fix suggestions and raw inspect export without requiring DevTools; trace steps are expandable with channel/port/dependency details, latest matching runtime event payloads, canvas focus and node inspect actions. Agent Runtime node inspect now renders a readable node summary with collapsible raw JSON instead of opening directly into a raw dump.
- Agent Runtime safe fix suggestions now diagnose broken/duplicate links, invalid ports, isolated nodes, agent bridge gaps, preview reachability and ambiguous roots; each suggestion shows problem, cause, action, risk and preview, and safe link/mapping fixes can be applied from the dialog before an automatic trace refresh.
- Agent Runtime v1.2 adds Undo Fix through time-travel snapshots, Runtime Fix Log, trace mode controls (`dry-run`, `simulate`, `execute-controlled`) and safe Agent Bridge creation from the palette when an agentic flow is missing the bridge boundary.
- Runtime docs now include `docs/ai/runtime/agent-runtime.md`, documenting that Trackers Lens builds its own Agent Runtime instead of embedding Codex directly; UI localization remains a later layer.

Flow Chat Brain phase 1 is implemented:

- AI Flow Chat now has a `flow-chat-brain/v1` read-only answer layer for advice/explain/memory-style prompts, using verified runtime context, workspace memory, a small built-in Trackers Lens RAG knowledge base and the configured local/OpenAI-compatible provider when available.
- The Brain RAG now attempts to fetch focused project docs (`AI.md`, project state, architecture, Flow Map overview, Prompt Chat, Safe Executor, Runtime Graph and AI Memory) from the same origin, with the built-in knowledge base as fallback when docs are unavailable.
- Brain activation is gated: simple inventory/count queries stay local, while advice and explanatory questions about nodes, edges, channels, config, architecture or best practices can use LLM + RAG.
- Brain output is strict JSON and is validated locally: recommended nodes must exist in the real Flow Map palette, mutation plans are not executable from Brain output, and existing safe executor/planner authority remains unchanged.
- Flow creation planning now also receives Brain context: workspace memory, docs RAG, runtime facts and palette are included in the AI planner prompt, and AI-created nodes are normalized only when they match real palette labels/subtypes instead of silently becoming custom unsupported nodes.
- LLM/provider failures now surface as user-facing Flow Chat warnings instead of only console errors: unreachable Ollama/LM Studio, missing provider/model, timeout/auth/context issues are normalized with direct setup hints while local fallback continues where possible.
- Flow creation plans now use port intelligence instead of always taking the first output/input: `Orchestrator Agent -> Agent Bridge` prefers `action -> agent_control`, `Agent Bridge -> Split/Preview` prefers `action -> input`, Task Node uses `task -> task`, and explicit AI-planner source/target ports are preserved when valid.
- Brain advice now exposes safe quick actions in the report UI: validated recommendations can launch follow-up prompts such as `Crea pattern agentico`, `Inserisci Agent Bridge`, `Crea boundary Flow In/Out` or routing patterns. These actions prepare a normal Flow Chat plan and still require the existing Create flow confirmation.
- Flow creation plan cards now expose visible refinement options before the final create action. Agentic plans can be rerouted with Flow In/Out, Condition, Split, a simplified Bridge pattern or an explanation pass; each option reruns analysis and still requires the final `Crea flow` confirmation.
- Flow Chat messages now support explicit feedback memory: useful assistant answers/plans can be marked `Memorizza`, which stores a confirmed `flow-chat-approved-pattern` workspace memory with prompt, validated nodes, links, planner metadata and sources; `Dimentica` removes that workspace memory directly from the chat, while `Non usare` marks the chat message without writing it as reusable memory.
- Flow Chat message text now renders a safe markdown subset for professional readability: bold labels, inline code, numbered steps and bullets are rendered as DOM nodes without using raw HTML.
- Flow Chat now applies the golden language rule across Brain, planner and conversational replies: the answer must use the same language as the user request. Italian explanation prompts such as `mi spieghi...` are classified as read-only `explain` instead of creation plans.
- Flow Chat Brain diagnostics are now collapsed by default under `Brain details`, showing intent, detected answer language, provider/model, confidence, RAG sources, workspace memories and validated palette nodes without cluttering the main answer.
- Flow Chat messages now expose a compact `Migliora` action. It reuses the previous user prompt for routing, passes the prior answer only as Brain refinement context, then appends a new improved response without deleting the original or adding a duplicate user message; this prevents explanatory answers from becoming accidental creation plans during refinement.
- Flow Chat refined responses are now traceable: messages created from `Migliora` show a `Revisione` badge, expose an icon action to jump back to the original answer, briefly highlight the source message, and include the source id in collapsed Brain diagnostics.
- Flow Chat now has post-response controls for turning an answer into a flow plan, saving it as an approved pattern, reusing it as a prompt template, opening Brain details and forcing a more operational rewrite with nodes, ports and links. The chat header also exposes a `Pattern salvati` dialog backed by workspace AI memory, with search, reuse, create-from-pattern and delete actions for approved memories plus visible rejected memories.
- Flow Chat creation now uses an `Applica con controllo` preflight panel before applying plans. It shows what will happen, validates palette nodes, IN/OUT ports, duplicate links and destructive actions, blocks invalid plans, captures an undo snapshot before writing, and returns a post-apply report with created/reused counts, warnings and Undo when available.
- AI Flow Chat open/closed state is persisted per workspace in `localStorage`, so a Flow Map reload restores the chat only when it was previously open.
- Flow Chat plan cards are now denser: `Opzioni` is collapsed by default, successful `Applica con controllo` stays compact unless warnings/blockers exist, and secondary post-response actions live under an `Altro` menu while primary create/save actions remain visible.
- Flow Chat approved-pattern memory is now smarter: similar `flow-chat-approved-pattern` records are retrieved per prompt, passed into Brain/planner as `approvedPatterns`, shown in plan cards as `Pattern simili`, and can be reused to regenerate a flow from a user-confirmed pattern.
- Flow Chat v2 runtime-tool bridge is started: Brain/runtime query context can include Agent Runtime `inspectFlow`, `suggestFixes`, recent runs and optional dry-run trace; Provider Health is summarized for LLM/provider questions; Brain RAG now includes Agent Runtime and Runtime Contract docs; memory ranking considers recency, frequency, workspace fit and stale penalties; explain-only prompts are blocked from planning.
- Flow Chat negative memory is now active: `Non usare` stores a `flow-chat-rejected-pattern` workspace memory, Brain/planner receive `rejectedPatterns`, similar approved patterns are filtered out, Brain details show `pattern ok/no`, plan cards show when memory guard excluded rejected patterns, and rejected memories can be deleted from the `Pattern salvati` dialog without DevTools.
- Flow Chat now carries compact same-chat conversation context into Brain, planner and conversational replies. Follow-up prompts such as `rifallo`, `come prima`, `senza Split`, `aggiungi Condition`, `spiegami il secondo nodo` or `usa Flow In/Out` can reference the previous plan; plan-follow-up requests are routed back to the planner instead of becoming plain text answers, fallback plans adapt the previous plan and apply constraints such as Flow In/Out boundary with valid parallel Preview output, replacing `Task Node` with `Flow In` for reusable boundary plans.
- Flow Chat AI planner now has a bounded self-repair loop before local fallback: provider output is parsed as JSON, normalized, analyzed and passed through the real preflight; invalid JSON, unsupported nodes or blocked ports are sent back to the provider with exact errors for up to three attempts. Planner activity now updates live during generation/repair, and final plan cards show a collapsed `Self repair` log plus `self-repair N` in the planner badge without duplicating it in the natural-language summary.
- Flow Chat now understands short follow-up commands such as `crealo`, `applica questo`, `procedi` or `usa questo piano`: it recovers the latest valid plan from the same chat, reruns the real preflight, applies it through the existing create/undo path for explicit apply commands, or reloads it as a ready plan for softer reuse commands.
- Flow Chat post-apply results now close the loop with a professional completion card: `Obiettivo raggiunto`, original goal, nodes/links applied, warnings and the next useful action are shown above the compact technical counts and Undo controls.
- Flow Chat hardening pass adds pattern memory quality scoring, a compact `Capo lavoro` plan summary, clearer blocked stale-plan messaging for short apply commands, collapsed post-apply technical checks when clean, and a `runFlowChatHardeningTests()` debug test helper.
- Flow Chat Brain answer validation no longer hard-cuts responses at 1400 characters. Brain answers allow longer complete explanations, prefer trimming on sentence/list boundaries, and the Brain prompt now requires numbered lists to complete every requested point.
- Advice prompts such as "which node should I insert for agents and IN/OUT edges" route to architecture advice instead of canvas inventory, with `Agent Bridge`, `Orchestrator Agent`, `Split`/`Condition` and `Flow In`/`Flow Out` guidance shown as validated recommendations.
- The Flow Chat report UI now exposes a `Flow Chat Brain` debug section with LLM/fallback mode, model/error, RAG count, memory count, confidence and validated recommended nodes.

Document Store multi-document replay and document enable controls are implemented:

- Document Store/Text Knowledge/Memory document replay now syncs disabled documents by removing their derived records on Play, then emits enabled stored documents incrementally by default; the `Replay all documents` toggle is available directly on the node and in settings, and forces all enabled documents to be sent again.
- Uploaded Knowledge documents now persist enabled metadata by default; the Documents dialog can toggle each document enabled/disabled without deleting it, and disabled documents remain visible but are excluded from replay.
- Documents dialog actions now have distinct colored treatments for upload, refresh, copy, view and delete, plus enabled/disabled state pills in the document list.
- Knowledge Graph and Graph Query now support real collection/workspace graph scopes: collection/workspace scope aggregates entities, relations and snapshots across all scoped documents instead of falling back to the latest document; new Knowledge Graph Test presets default the graph/query nodes to collection scope.
- Knowledge Graph nodes expose a manual `Clear Graph` index action for scoped entities/relations/snapshots and an `Auto clear snapshots` config toggle that clears only old graph snapshots once per run without deleting freshly extracted entities.
- Knowledge Graph/Graph Query now normalize saved legacy configs where `graphScope=document` but `documentId` is empty: if a `collectionId` exists, the effective scope becomes `collection`, preventing old sample nodes from collapsing back to the latest document.
- Disabled Knowledge documents are non-destructive when toggled, blocked from future replay, and pruned from derived chunks/entities/relations/graph mappings on the next Document Store Play.
- Deleting a Document Store with existing derived knowledge now opens a dedicated confirmation dialog: users can delete only the node or also clear the generated graph mapping for that node's documents.

Knowledge Graph multilingual quality baseline is complete:

- Knowledge runtime now has language profiles for Italian, Spanish, English, French and German with language-specific stop words and weak sentence-start tokens.
- Documents and chunks now persist detected/preferred language metadata, so graph extraction/query can carry language scope across the pipeline.
- Entity extraction, graph snapshots and Graph Query now use language-aware stopword/weak-label filtering while keeping the previous legacy stopword set as a conservative safety net.
- User-verified multilingual graph exports for Italian, Spanish, English, French and German after language-specific cleanup passes.

Knowledge Graph Query ranking refinement is complete:

- Graph Query now enriches returned entities with `score`, `connections` and `matched` metadata.
- Graph Query now enriches returned relations with labels, direct-match flags and relation scores for cleaner AI/Preview payloads.
- Definitional queries such as "chi e X?" prioritize direct relations around the matched entity before generic `co_occurs` neighbors.
- Graph Query evidence now scans document chunks containing the matched entity and clips snippets around that entity, so definitional answers get local evidence instead of only relation-neighborhood text.
- Graph Query seed scoring now requires an actual lexical match before adding confidence, preventing unrelated high-confidence entities from becoming query seeds.
- Definitional Graph Query relation ranking now demotes location-only relations and boosts direct person, quote and concept neighbors.
- User-verified `chi e Liber?` / `chi è Liber?` now ranks `Liber` as the only query seed, returns focused direct relations and supplies definitional evidence first.
- Knowledge Graph cleanup follow-up is complete and user-verified: entities described by local source/book/document/work cues are typed as `source`, `source -> source` pairs become `references` before generic relation fallback, and the Knowledge Graph sample test fails with explicit warnings if weak Italian labels such as `Doveva`/`Viene` or source/source `co_occurs` pairs remain after cleanup.

Semantic Relation Enricher base is implemented and browser-verified on the Knowledge Graph export:

- Flow Map palette now includes `Semantic Relation Enricher` as a Knowledge node with relation/graph/chunk inputs and semantic/enriched/context outputs.
- Knowledge runtime enriches base relations into semantic relations stored in `tl_knowledge_relations` with `metadata.semantic`, `metadata.originalRelationType`, `evidence`, `extraction` and explanation metadata.
- Rule fallback covers initial semantic types including `friend_of`, `helps`, `tries_to_help`, `healed_by`, `cannot_speak`, `has_property`, `lives_in`, `seeks`, `protects`, `opposes`, `causes`, `leads_to`, `is_part_of`, `teaches`, `discovers`, `asks_for`, `receives_from` and `gives_to`.
- Rule fallback was tightened after a French graph export showed broad chunk-level false positives: friendship now requires both person entities near a friendship cue, healing/property cues are localized around object/concept targets, and French apostrophe starts such as `C'était` are filtered as weak entities.
- A follow-up French export reduced semantic relations from 22 noisy records to 6, with weak entities removed; remaining false positives around `cannot_speak`, `healed_by` and `has_property` were addressed by requiring the patient before the semantic cue and by skipping property relations for location/object pairs.
- Knowledge Graph Test no longer pins downstream Semantic/Graph/Graph Query nodes to the fixed sample `documentId`; those nodes use collection/latest-document scope so uploaded `kdoc_*` documents do not show as `documentMismatch`.
- Latest French export now shows 22 entities, 65 relations, 2 clean semantic relations (`friend_of`, `healed_by`), no weak entities, no known bad semantic orientation and `documentMismatch: false`; View Graph/Debug treats the sample's synthetic configured document id as a valid latest-snapshot fallback instead of reporting a mismatch.
- Italian export verified entity cleanup and document fallback (`documentMismatch: false`) but exposed over-broad semantic enrichment for `opposes`, `gives_to`, `asks_for`, `lives_in` and `seeks`; semantic rules are now stricter about typed source/target pairs and cue position, and base narrative `opposes` no longer fires on generic concept/location pairs.
- Follow-up Italian export reduced semantic noise to 3 records but still produced false `healed_by` relations with `bastone` and false `opposes` between `Alberi Secchi` and `bastone`; `healed_by` now requires healing-like object labels and excludes weapon/tool labels, while semantic `opposes` now requires a creature antagonist or explicit oppositional concept.
- Latest Italian export after `semantic-rel-5` shows 21 entities, 39 relations, `semanticRelationCount: 0`, no weak entities, no source/source `co_occurs`, no biblical residue, no known bad semantic relations and `documentMismatch: false`; semantic enrichment now prefers no relation over false positives for this story.
- English technical/RAG export exposed weak heading/pronoun entities such as `We'll`, `How`, `Who`, `You` and `Powered`, which also created false `seeks`/`helps`; English weak filters now cover these heading/pronoun tokens, contraction cleanup removes pronoun contractions, and semantic `seeks` ignores weak targets.
- Optional AI enrichment can use existing local/OpenAI-compatible provider profiles through the same localhost chat proxy path, but rule fallback remains the stable path.
- Knowledge Graph snapshots now carry `semanticRelationCount`; Graph Query ranks semantic relations higher and includes method/original/evidence hints in graph context.
- Knowledge Graph Test now inserts `Semantic Relation Enricher` between Entity Extractor and Knowledge Graph and fails if semantic relations are not ready before Graph Query.
- View Graph/Knowledge Graph Debug now surface semantic relation counts, semantic relation colors and relation method/original/evidence details.

Knowledge Graph Builder Agent base is implemented and pending provider verification:

- Flow Map palette now includes `Knowledge Graph Builder Agent` as a Knowledge node with chunk/entity/relation/graph inputs and `knowledge.graph.proposed`, `knowledge.semantic.relations`, `knowledge.graph.enriched` and `knowledge.graph.context` outputs.
- The runtime calls the existing local/OpenAI-compatible AI provider layer through the same chat proxy path and requires strict JSON proposals with entities, relations, aliases and rejected candidates.
- Builder proposals are validated before persistence: accepted entities and relations require exact evidence quotes from selected chunks, allowed relation types are schema-limited, weak labels are rejected, confidence thresholds are enforced and duplicate semantic relation keys are skipped.
- Accepted builder relations are stored in `tl_knowledge_relations` with `metadata.semantic`, `metadata.graphBuilder`, `extraction.method = ai-graph-builder`, provider/model metadata and evidence, so Knowledge Graph, Graph Query and Debug can consume them without a parallel store.
- Technical relation types such as `uses`, `implements`, `explains`, `stores_in`, `retrieves_from`, `powered_by` and `depends_on` are supported for non-narrative documentation while still requiring evidence.
- Provider hardening after LM Studio returned a bare `HTTP 400`: OpenAI-compatible local models are now resolved from `/v1/models`, 400 responses retry once with a compact single-chunk prompt, and fallback payloads include the provider response body so the Preview exposes the real cause instead of only the status code.
- Post-LLM technical normalization is now active for the Builder: generic `helps` proposals can be converted to technical relations such as `interfaces_with`, `connects_to` or `uses` when evidence supports it, username/password atoms are compressed into `connection credentials`, and reruns replace only prior `metadata.graphBuilder` records for the same document so stale AI graph output does not pollute new previews.
- Italian narrative Builder verification exposed a false `ilenzio` proper-noun target for `Juliette -helps-> ilenzio`; Builder validation now rejects lowercase/quote-only proper-noun candidates and requires narrative relations such as `helps`/`friend_of` to connect compatible entity types whose labels are present in the evidence neighborhood.
- Local small-context provider verification exposed LM Studio `Context size has been exceeded`; the Builder now retries AI graph extraction as `full -> compact -> micro`, with `micro` sending one short chunk and no existing candidates, and exposes `promptMode` in Preview/extraction metadata.
- English pure Builder preview now succeeds with LM Studio/Gemma in `promptMode: full`, accepting clean `Juliette -friend_of-> Liber` and `Juliette -seeks-> Cure` relations while rejecting weak candidates; `cannot_speak` validation now allows a `Speech`/`Voice` concept target when the evidence contains a speech-inability cue such as "could not speak".
- A follow-up Builder preview showed `status: ready` with zero raw entities/relations after an empty AI proposal; AI calls now retry empty/invalid JSON responses through the same `full -> compact -> micro` ladder and report `empty-ai-proposal`/`invalid-ai-json` as fallback diagnostics if all attempts remain empty.
- Latest English Builder preview produced a richer graph but exposed residual narrative typing issues: physical attacks were proposed as `causes`, `seeks` targeted `Liber` instead of the cure, and `cannot_speak` targeted `NEURON Forest`; Builder normalization now maps attack-like `causes` to `opposes`, restricts `seeks` targets to non-person goal entities, and requires `cannot_speak` targets to be Speech/Voice-like concepts.
- Knowledge Graph snapshot export showed no `metadata.graphBuilder` records but exposed rule-enricher noise: `cannot_speak -> compassion`, `friend_of -> NEURON Forest` and broad `discovers` relations. Rule enrichment now filters non-person proper nouns from social/speech relations, requires Speech/Voice-like targets for `cannot_speak`, and narrows `discovers` to explicit allowed targets after a discovery cue.
- Latest English Knowledge Graph export after cleanup shows `semanticRelationCount: 8` and `graphBuilderRelationCount: 4`; previous false `cannot_speak -> compassion` and `friend_of -> NEURON Forest` are gone, while Builder relations are clean (`has_property`, `helps`, `opposes`). The remaining broad rule-based `discovers` edges are now further constrained to immediate discovery targets, generic `creature` labels are rejected as discovery targets, and the Flow Map/worker cache is bumped to `graph-builder-10`.
- Follow-up English export with the refreshed runtime confirms broad rule-based `discovers` edges are gone (`semanticRelationCount: 2`, no suspicious semantic relations). It exposed a weak narrative Builder relation, `Juliette -depends_on-> Liber`; Builder validation now treats technical relation types such as `depends_on` as technical-only unless evidence/labels contain technical context, so person-to-person narrative dependency edges are rejected under cache `graph-builder-11`.
- Follow-up export after `graph-builder-11` verifies `depends_on` is gone and `suspiciousCount: 0`, but the Builder accepted weak abstract relations from one quote: `discipline -connects_to-> determination` and `determination -leads_to-> courage`. Builder validation now requires technical context for all technical relation types on non-technical entities, and requires explicit causal cue plus both labels inside the quote for `causes`/`leads_to`, under cache `graph-builder-12`.
- Latest export after `graph-builder-12` verifies the abstract `connects_to`/`leads_to` false positives are gone: 33 entities, 94 relations, 3 semantic relations, 2 Builder relations (`Juliette -has_property-> determination`, `troll -opposes-> Juliette`) plus the rule `Juliette -friend_of-> Liber`, with no known suspicious semantic relation.
- Technical/RAG export with a different file verifies technical relation types are no longer over-blocked: 53 entities, 82 relations and 5 Builder semantic relations with no suspicious narrative carryover. It exposed two precision improvements now handled under cache `graph-builder-13`: `has_property` with `works for` evidence normalizes to `works_for`, and `uses` is re-oriented when the quote says `Using X, Y applications...` so `Y -uses-> X`.
- Builder preview after `graph-builder-13` verifies `works_for` normalization directly: `Michael -works_for-> prismaticAI` and `Sarah -works_for-> prismaticAI` are accepted with exact evidence and no suspicious relations. The same preview exposed rejected technical loader/splitter relations due to missing vocabulary, so Builder relation types now include conservative technical verbs `loads`, `splits`, `splits_into`, `processes` and `transforms` under cache `graph-builder-14`.
- Full Knowledge Graph export after `graph-builder-14` verifies the persisted graph remains conservative and clean: 52 entities, 79 relations, 2 semantic Builder relations, both `works_for`, and no suspicious semantic relation. Loader/splitter semantic relations are not present in this export because accepted target entities such as `documents`/`text data` were not in the graph, so the new technical vocabulary is available but still evidence/entity-gated.
- Italian mini-story export verifies narrative Builder quality remains mostly clean after the technical vocabulary work: 21 entities, 44 relations and 5 Builder semantic relations with no old false positives. It exposed two precision fixes now handled under cache `graph-builder-15`: symmetric relations such as `friend_of` are deduplicated by unordered pair, and `opposes` rejects weak dismissal evidence such as "ignored her words" unless there is a strong attack/confrontation cue.
- Follow-up mini-story export after `graph-builder-15` produced zero semantic relations, revealing a rerun dedup bug rather than a quality target: `replaceExisting` deleted previous Builder records, but the dedup key set was still built from the pre-delete `relationsAll` snapshot and skipped newly proposed relations as already existing. `graph-builder-16` excludes stale same-document `metadata.graphBuilder` records from dedup when replacement is enabled.
- Rerun after `graph-builder-16` confirms the replace/dedup bug is fixed, but output became too generic: 3 Builder semantic relations (`contains`, `mentions`) with no false positives but missing high-signal narrative relations. `graph-builder-17` updates the Builder prompt to prefer explicit narrative semantics over generic `mentions`/`contains`, adds `encounters` to Builder relation types, normalizes encounter-like `mentions` to `encounters`, and restricts `mentions` to source/document/technical-reference contexts.
- Rerun after `graph-builder-17` still produced only generic `contains` relations. `graph-builder-18` adds a narrow post-AI supplemental rule layer inside the Builder: after entity acceptance and before relation validation, explicit friendship/bond cues can add `friend_of`, and explicit speech/revelation cues around a quote entity can add `reveals`; these supplemental relations still pass the same evidence quote, entity and dedup validation as AI-proposed Builder relations.
- Rerun after `graph-builder-18` produced `encounters` and a weak AI `friend_of` based only on travel together. `graph-builder-19` tightens `friend_of` compatibility to require an explicit friendship/bond cue and improves supplemental `reveals` evidence selection by using the context between speaker and quote when one sentence does not contain both labels.
- Rerun after `graph-builder-19` showed the user's overfit concern clearly: the supplemental friendship fallback was generic, not hardcoded to Liber, but it treated location-like proper nouns such as forests/trees/castles as people and paired every person-like entity in a chunk when it saw `amica`. `graph-builder-20` makes the fix category-based: non-person proper nouns now include common location/object labels in Italian/English, supplemental `friend_of` requires a direct evidence sentence/context for the specific pair, and supplemental `reveals` no longer falls back to the first person in a chunk as speaker.
- Rerun after `graph-builder-20` verifies the false location `friend_of` edges are gone, but supplemental `reveals` still chose a speaker mentioned after the quote. `graph-builder-21` makes speech attribution directional: supplemental quote speakers must appear before the quote and the speaker-to-quote context must contain a speech/voice cue.

Knowledge Dictionary direction is now the next architecture path:

- Do not delete the Semantic Relation Enricher or Knowledge Graph Builder Agent work. Keep it as a validated graph producer, provider/proxy integration, evidence validator, graph persistence layer and debug/test harness.
- Freeze growth of case-specific graph-builder micro-rules. Future lexical/type precision should move into a general Knowledge Dictionary layer instead of adding story-, book- or prompt-specific exceptions to relation validation.
- Reuse existing stores and surfaces where possible: documents, chunks, language profiles, entities, relations, snapshots, graph context, AI provider profiles, Preview, View Graph and Knowledge Graph Debug.
- New target pipeline: `Document -> Language Profile -> Knowledge Dictionary -> Entity/Concept Typing -> AI/Rule Graph Builder -> Validator -> Knowledge Graph -> Graph Query/AI`.
- Dictionary scope must be isolated by default: document-level entries first, with optional collection/workspace/global language promotion only when explicitly requested. Different Flow Maps and Knowledge Graphs must not share learned terms unless they are intentionally linked by workspace/collection policy.
- Proposed Dictionary record shape should cover `workspaceId`, `flowId`, `collectionId`, `documentId`, `language`, `term`, `lemma`, `aliases`, `typeCandidates`, `semanticHints`, `relationCues`, `confidence`, `evidence`, `source`, `createdAt` and `updatedAt`.
- Timeline:
  1. Phase 0: document the architecture pivot and freeze graph-builder rule expansion except generic safety validation.
  2. Phase 1: add local Dictionary store/model and cleanup behavior tied to document deletion.
  3. Phase 2: add a Knowledge Dictionary Builder node that emits `knowledge.dictionary.updated` and `knowledge.lexicon.context`.
  4. Phase 3: make Entity Extractor and Graph Builder consume dictionary hints for typing, aliases and ambiguity resolution.
  5. Phase 4: add Dictionary Debug/View/export so accepted/rejected lexical knowledge is inspectable.
  6. Phase 5: add optional provider-backed dictionary enrichment with rule fallback and strict evidence validation.
  7. Phase 6: add explicit cross-document/collection merge policy for promoted dictionary knowledge.
- Phase 1/2 base is now implemented under cache `knowledge-dict-1`: `tl_knowledge_dictionary` is part of the Knowledge store set, document regeneration/deletion cleans document-scoped dictionary entries, the palette exposes `Knowledge Dictionary Builder`, and the runtime emits both persisted dictionary records plus `knowledge.dictionary.updated` / `knowledge.lexicon.context`. The first implementation is local/rule-based and evidence-linked; Entity Extractor and Graph Builder do not consume the dictionary yet.
- First Italian preview verified persistence and scoping (`language: it`, `scope: document`) but exposed over-broad dictionary extraction: sentence-start/function words such as `Certo`, `Nonostante`, `Tuttavia`, `Accesero` and `Corsero` were accepted as high-confidence `proper-noun` entries. `knowledge-dict-2` tightened function-word/sentence-start filtering, added generic lexical typing for common location/object/role terms and stripped leading articles from lemmas. Runtime/debug surfaces must preserve full payloads instead of compacting or truncating data.
- Runtime routing bug fixed under `knowledge-dict-3`: non-source Knowledge nodes without incoming visual dependencies no longer consume global channel events by default. This prevents disconnected nodes such as `Knowledge Graph Builder Agent` from starting local AI work when `Graph Document Store` or `Chunk Processor` emits on `knowledge.document.created` / `knowledge.chunk.created`; only source-like document/memory nodes keep unlinked event intake, while other nodes require a matching dependency or explicit `targetNodeId`.
- Follow-up Dictionary preview is cleaner but still showed weak low-value Italian tokens (`sempre`, `solo`, `avrebbe`, greetings/quote starts such as `Buongiorno`/`Ciao`, and verb-like terms such as `parlava`). `knowledge-dict-4` filters these generic function/greeting/verb/adjective tokens, prevents weak multi-word labels when any component is weak, adds concept typing for `voce`/`cura`/`silenzio`-style terms, and caps `dictionaryEntryIds` in Preview with a truncation flag so the full store can remain complete without flooding Preview.
- Dictionary ranking added under `knowledge-dict-5`: each entry now carries `tier` (`core`, `typed`, `context`, `weak`), `usableAsSeed`, and `seedScore`. Preview includes `tierCounts` and `usableSeedCount`, and the context lines show whether each term is seed-eligible. This keeps the Dictionary useful as lexical context while preventing generic terms from becoming graph/entity seeds in the next integration step.
- User-provided `knowledge-dict-5` preview verifies the tier split is usable: `Juliette`, `Liber`, `Scoraggiamento` and `NEURON` are `core`; `Montagna`, `foresta`, `fiore`, `anziano`, `luogo`, `acqua`, `castello` and `caverna` are `typed`; generic terms such as `parlare`/`viaggio` remain `context`; weak terms such as `amico`, `cammino`, `cuore`, `destra` and `notte` are not seed-eligible. Current output shows `tierCounts: core=9, typed=21, context=2, weak=88` and `usableSeedCount: 30`, which is a good gate for the next Entity Extractor integration.
- Entity Extractor dictionary integration is implemented under `knowledge-dict-6`: the palette now accepts `knowledge.dictionary.updated` / `knowledge.lexicon.context`, the runtime reloads same-document chunks on dictionary events, and only `usableAsSeed === true` entries at `core`/`typed` tier are injected as `dictionary-seed` candidates. Extracted entities preserve `metadata.dictionary` with tier/seed score, while `context` and `weak` dictionary terms remain non-seed lexical/debug material.
- Dictionary-to-Entity routing follow-up fixed under `knowledge-dict-7`: dependency event matching now considers all saved dependency channel fields (`channel`, source/target ports and metadata aliases) while ignoring wildcard `all`, so a visual link from `Knowledge Dictionary Builder` to `Entity Extractor` can pass `knowledge.dictionary.updated` or `knowledge.lexicon.context` even when the canvas stores the visible port as source/target metadata.
- Dictionary LLM/hybrid hardening is active under `dictionary-llm-json-1`: LM Studio/Gemma dictionary output now uses a more tolerant JSON object parser, can recover source-validated dictionary candidates from non-JSON model text without an extra repair POST, and local/hybrid typing recognizes generic narrative creature/threat terms such as troll/mostro/nemico/pericolo/paura/scoraggiamento as high-signal dictionary material instead of losing them among weak fallback tokens.
- Dictionary LLM coverage follow-up is active under `dictionary-llm-coverage-1`: local-provider global prompts still stay small to avoid LM Studio context overflow, but pure `dictionaryMode=llm` now scans more source chunks one at a time through a bounded chunk pass so later document terms are not missed just because the first two chunks already produced a few candidates.
- Dictionary LLM coverage/quality follow-up is active under `dictionary-llm-coverage-2`: chunk-pass scanning no longer stops as soon as raw candidates reach `maxTerms`; it covers the bounded source window and only then merges/slices. Salvaged non-JSON terms now require exact label occurrence in a source chunk, preventing substring artifacts such as English `forest` inside Italian `Foresta` or dangling fragments such as `sorgente d`.
- Dictionary LLM anchor follow-up is active under `dictionary-llm-anchors-1`: pure `dictionaryMode=llm` still calls the configured provider, but its final dictionary now merges LLM output with source-validated typed anchors from the document so obvious evidence terms such as `bastone`, `paura`, `pericolo`, `Scoraggiamento` and `troll` are not lost when the model omits them. AI candidates also require exact label occurrence instead of normalized substring matching, blocking chunk-overlap fragments such as `mento`/`opo`.
- Chunk Processor boundary follow-up is active under `smart-chunk-boundaries-1`: Knowledge chunking no longer slices text only by fixed character offsets. It now prefers paragraph/sentence/whitespace end boundaries and moves overlap starts to word starts, preventing downstream Dictionary/Event/Graph nodes from receiving chunks that begin with cut words such as `lcosa`, `ndolo`, `mento` or `opo`.
- Paragraph Chunk Processor follow-up is active under `paragraph-chunker-1`: the configured chunk `strategy` now reaches the runtime splitter. `strategy=paragraph` / `markdown` aggregates complete paragraphs up to `chunkSize`, falls back to sentence/word-safe splitting only for oversized paragraphs, and avoids character overlap between normal paragraph chunks so boundaries stay clean.
- Structured Chunk Processor follow-up is active under `structured-chunker-1`: the old character-boundary splitter has been replaced by a deterministic structured chunker with estimated token budgets, block/page/section metadata, controlled overlap and word-safe splitting only for oversized blocks. Legacy `fixed` / `paragraph` / `markdown` configs normalize to `structured`, while new palette/sample configs and the Configure dialog expose `structured|section|token` plus explicit token budget fields.
- Dictionary evidence-pack follow-up is active under `dictionary-evidence-pack-1`: Dictionary records now store an ordered `evidencePack` per term, built from all exact term/alias occurrences across scoped chunks sorted by document ordinal/start. Preview and Agent `defineTerm` expose this pack so tools can reason over term context in document sequence instead of a single primary chunk quote.
- Dictionary evidence-pack dedup follow-up is active under `dictionary-evidence-pack-2`: evidence packs deduplicate labels by normalized alias and evidence by chunk/absolute offset, preventing duplicate snippets when a term and alias resolve to the same source occurrence.
- Dictionary ranking follow-up is active under `dictionary-rank-anchors-1`: LLM and source-anchor candidates now merge with a typed/evidence-aware rank instead of always putting AI candidates first, so source-grounded high-signal anchors such as object/concept/creature terms are less likely to be hidden behind weaker LLM fragments. Dangling connector labels ending in `dell`/`della`/`di`-style tokens are filtered as weak dictionary entries.
- Dictionary LLM purity follow-up is active under `dictionary-llm-pure-1`: pure `dictionaryMode=llm` no longer merges source-anchor candidates into final entries. The configured LLM/system prompt owns term selection; TL only validates exact source evidence, builds ordered `evidencePack`, persists metadata and keeps local anchors/fallback for `hybrid`/rules paths.
- Dictionary quality pass implemented under `knowledge-dict-8`: the Dictionary/Extractor now uses generic lexical gates for common nature/place/object terms, common modifiers and anaphoric fragments, preventing capitalized common words from becoming `core/proper-noun` seeds. Entity extraction triggered by Dictionary context now runs in a more conservative relation mode with lower default relation fan-out and only emits base relations that have a safe structural type or explicit narrative cue, leaving strong semantic relations to the Semantic Enricher / Graph Builder.
- Entity Extractor cleanup follow-up is active under `entity-extractor-clean-1`: story/domain-specific entity canonicalizations and keyword entity lists have been removed from the base extractor, and rule fallback now emits only conservative structural relation types. Domain terms must come from `seedTerms`, Dictionary seeds, custom rules or the LLM path; semantic relation inference belongs to Semantic Relation Enricher / Graph Builder.
- User retest after `knowledge-dict-8` shows the intended direction: graph export dropped from 39 entities / 115 relations to 34 entities / 57 relations, `Alberi` / `Alberi Secchi` are now `typed/location` instead of `core/proper-noun`, and anaphoric `L'altra` is gone. Remaining issue: Dictionary-triggered Entity Extractor still emitted `reveals` relations directly from narrative cues. `knowledge-dict-9` now ignores high-meaning narrative relation labels during Dictionary-driven extraction and emits only conservative base relation types for those pairs, so semantic labels must come from Semantic Enricher / Graph Builder.
- Graph Query / AI context connected-mode implemented under `knowledge-dict-10`: graph context now defaults to connected entities only when relations are available, reports connected/isolated candidate counts, and exposes `includeIsolated` as an explicit debug option. Relation ranking now gives an extra boost to relations with evidence/quotes/explanations, so semantic evidence-bearing relations are prioritized in `knowledge.graph.context` before weaker base edges.
- Graph Query config UI migration fixed under `knowledge-dict-11`: existing Knowledge nodes with an older saved `settingsSchema` no longer bypass current runtime field definitions, so existing `graph-query` nodes show the new `Include isolated entities` toggle without recreating the node.
- Graph Query answer-evidence expansion added under `knowledge-dict-12`: when a query matches multiple graph seeds or asks an instrument/action question, relations from high-match chunks receive a bounded context expansion so answer-critical nearby objects/evidence can enter `knowledge.graph.context` without adding document-specific graph rules.
- Graph Query answer-evidence expansion tightened under `knowledge-dict-13`: real AI debug output answered the instrument question correctly but showed `answerExpansionChunkCount` was too broad, so high-match chunks now require at least two matched seeds when the query has multiple graph seeds, falling back to single-seed expansion only for genuinely single-seed questions with additional query-token evidence.
- Semantic action-object extraction added under `knowledge-dict-14`: `uses` is now a first-class semantic relation for evidence-backed person/object action contexts. The Semantic Relation Enricher can derive `person -uses-> object` from existing base pairs or from strict same-chunk supplemental evidence, and the Graph Builder validator now accepts narrative `uses` only when explicit action cues support it.
- AI Agent graph-context consumption fixed under `knowledge-dict-15`: the clean AI payload and prompt block now preserve semantic/direct/method/original/evidence metadata for Graph Query relations and explicitly instruct the model to answer from direct semantic evidence instead of reporting missing evidence when the relation and quote are present.
- `knowledge-dict-16` tightens action-object subject attribution and answer wording: supplemental `uses` skips a candidate person when another person appears between that candidate and the action cue, preventing false edges such as `Juliette -uses-> bastone`, and AI graph prompts now ask for natural translation of source-language object labels such as `bastone` when answering in English.
- `knowledge-dict-17` tightens AI graph answer language: Graph AI answers should use the same language as the user query and avoid parenthesized translations when the query and evidence are already in the same language; translations are reserved for cross-language answers or explicit translation requests.
- `knowledge-dict-18` makes query-language answering the stable Graph AI rule: the AI Agent must answer in the language of the user query, not the source document language, translating graph labels only as needed for a natural answer and avoiding parenthesized original terms unless explicitly requested or required for disambiguation.
- `knowledge-dict-19` improves healing semantics: Semantic Relation Enricher now adds strict evidence-backed `person -healed_by-> healing object` supplemental relations for speech/healing contexts, suppresses false `object -has_property-> voce` relations in healing/voice contexts, and Graph Builder accepted `healed_by` proposals are oriented as patient -> healer before persistence.
- `knowledge-dict-20` deduplicates overlapping healing objects during supplemental healing extraction, preferring the more specific label such as `acqua del fiume` over the shorter contained label `acqua` for the same chunk/evidence.
- `knowledge-dict-21` treats healing/voice questions as causal-chain queries: Graph Query boosts `causes`/`has_property` evidence in recovery questions, and Semantic Relation Enricher can derive `healing source/object -> causes -> voice/speech` when evidence mentions a curative source plus preparation/drinking/immersion cues. This keeps answers from collapsing a flower/water/cup healing chain into only `person -healed_by-> water`.
- `knowledge-dict-22` tightens healing-chain quality: causal `healing source/object -> causes -> voice/speech` evidence now requires the mechanism to appear before the outcome, preventing post-recovery objects such as a second flower from becoming the cause. Graph Query also boosts and snippets preparation/drinking chunks for healing questions, so evidence with cup/tea/flower/water/drinking can reach the AI even when the chunk does not repeat the final `voice` concept.
- `knowledge-dict-23` adds the first Dictionary Debug/View/export foundation: `Knowledge Dictionary Builder` nodes now expose an inspector panel backed by `tl_knowledge_dictionary`, filtered by workspace/document/collection/language, with tier counts, usable seed counts, type summaries, evidence previews and a copyable export payload. This makes accepted lexical knowledge inspectable before adding provider-backed dictionary enrichment.
- Flow Map AI/LLM nodes now expose per-node token accounting on the canvas: total tokens, last run tokens and an icon clear action. The clear action follows the existing node/children dialog pattern, and runtime token totals persist through AI Agent, Orchestrator Agent, Knowledge Event Builder, Semantic Relation Enricher, Knowledge Graph Builder Agent and provider-backed embedding/vector nodes.
- `knowledge-events-1` starts the Narrative Event layer so the system can answer ordered "how/why" questions without growing document-specific relation micro-rules. The Knowledge store now includes `tl_knowledge_events`, document regeneration cleans document-scoped events, the palette exposes `Knowledge Event Builder`, and the runtime emits `knowledge.events.updated` plus `knowledge.event.context`. The first extractor is conservative/rule-based with exact sentence evidence and sequence order; Graph Query and AI Agent now include ordered events in graph context, preferring event chains for how/why answers.
- `knowledge-events-2` tightens the first Event Builder preview: chunk ordering now uses `ordinal`/`start` instead of only `index`, bare `cura` no longer creates false `heals` events, `seeks` covers cure-search evidence, and subject attribution only accepts named subjects before the action cue with guarded fallback for pronouns/elliptic verbs.
- `knowledge-events-3` improves Event Builder signal quality after preview testing: speech state now uses `cannot_speak` instead of false `speaks`, title/fragments are ignored, modal/future healing statements such as "could/will be healed" no longer become completed `heals` events, and the Event Builder preview selects high-value causal events so cup/flower/tea/drink/speech chains are visible instead of hidden after the first chronological records.
- `knowledge-events-4` further tightens event precision: curative property statements now become `has_property` instead of completed `heals`, ambient filling such as a castle filled with music/laughter/hope is ignored as a material `fills` event, and subject fallback no longer treats articles/object pronouns such as `il/lo/la` as subject references.
- `knowledge-events-5` narrows `has_property` event triggers so generic auxiliary phrases such as "ha senso" no longer create property events; properties now require semantic property/power/capability cues such as `possiede`, `potere`, `proprietà` or `capacità`.
- `knowledge-events-6` separates compound material-action sentences into ordered event records when a sentence clearly contains both fill and immerse actions, so evidence such as "fill a cup with water and immerse the flower" becomes `fills` plus `immerses` instead of one mixed-object event.
- `knowledge-events-ai-1` wires the Knowledge Event Builder `extractionMode` to the existing local/OpenAI-compatible provider layer. `ai` and `hybrid` modes now request strict JSON events from the LLM, accept only schema-valid events with quotes found in source chunks, deduplicate against rule candidates, and fall back to rule events if the provider fails.
- `knowledge-events-ai-2` adds generic AI-event compatibility validation: `has_property` proposals require explicit property/power/ability evidence, failed speech/silence evidence is normalized from `speaks` to `cannot_speak`, and the Event Builder prompt now tells the provider to preserve source-language labels and separate successful speech from failed attempts.
- `knowledge-events-ai-3` tightens hybrid event deduplication so AI and rule candidates with the same chunk, event type and evidence quote collapse into one ordered event, reducing duplicate `cannot_speak`, `fills`, `immerses`, `transforms` and `drinks` records while preserving rule-only coverage.
- `knowledge-events-ai-4` makes Graph Query event retrieval chain-aware for healing/voice questions: preparation and mechanism events such as `finds`, `fills`, `immerses`, `transforms`, `takes`, `drinks`, `has_property` and final speech are explicitly merged into the structured event payload instead of losing to early `cannot_speak` matches.
- `knowledge-events-ai-5` polishes Graph AI answer wording: the AI Agent prompt now tells models to write idiomatic prose instead of literal graph-relation wording, especially for transforms/causes chains, reducing awkward phrases such as "causando alla soluzione" when the graph evidence itself is correct.
- `knowledge-events-debug-1` adds Event Debug/View/export parity with Dictionary Debug: `Knowledge Event Builder` nodes now expose an inspector panel backed by `tl_knowledge_events`, filtered by workspace/document/collection scope, with event type counts, extraction method counts, confidence summary, sequence range, timeline preview, evidence snippets and a copyable export payload.
- `knowledge-cleanup-1` fixes stale derived Knowledge data after multi-document tests: Document Store now has a parent `Clear Memory` action that removes derived chunks, embeddings, entities, relations, dictionary, events, queries and snapshots while preserving uploaded documents; Dictionary and Event Builder nodes have scoped `Clear Dictionary` / `Clear Events`; `Clear Graph` stays graph/query scoped; document deletion and workspace sample cleanup remove downstream derived records. Graph Query also ignores events whose document is no longer active in the current entity/chunk scope.
- `knowledge-normalization-1` starts the shared event-normalization foundation from the Knowledge Graph fix plan: Event Builder records now carry generic `roles`, `subjectResolution`, `polarity`, `modality` and `aspect` fields, and Graph Query / AI graph prompts preserve those fields. Pronouns and plural subjects are resolved only through explicit/contextual evidence, unresolved pronouns stay unresolved, Italian articles such as `il` are not treated as pronouns, objects are no longer reused as future narrative subjects unless the dictionary classifies them as person/role-like participants, immersion events preserve shared liquid/container context as `roles.destination`, and graph-answer prompts now require concrete ordered event chains for how/why answers instead of replacing them with broad summary relations or appending later aftermath/context when the user asked for the mechanism.
- `reasoning-composer-1` starts the answer-grounding layer needed for professional RAG/Memory quality: the Flow Map palette now exposes `Knowledge Reasoning Composer`, which consumes `knowledge.graph.context` / `knowledge.event.context`, emits `knowledge.reasoning.plan`, and forwards an enriched `knowledge.graph.context` with `reasoningPlan`. The first local composer detects answer intent and selects required event/relation facts with evidence. Later QA removed excluded-context/boundary output because TL must not constrain final answer semantics. AI Agent prompts treat the reasoning plan as higher priority than raw graph context. AI Agent also recognizes `knowledge.reasoning.plan` directly as graph/reasoning context so either Composer output can feed the answer node. Mechanism intent now builds a compact operational event window instead of ranking the whole story, suppresses competing raw graph prompt sections when a plan exists and keeps clean AI previews smaller. The Composer now exposes `primaryEvidenceText` from selected original evidence, using graph facts as navigation/verification rather than as a replacement for the original text.
- `reasoning-composer-2` improves answer quality for generic "how/details/process" questions: Graph Query now detects process intent and adds a bounded evidence-backed predecessor/outcome event window before Reasoning Composer runs, while the Composer keeps a small prelude before the operational mechanism chain. The Composer now prefers the outcome event that best matches the question goal (for example speech/voice outcomes over an earlier drink step), augments `primaryEvidenceText` with focused Graph Query source excerpts when the plan would otherwise hide useful evidence, and passes response instructions to AI Agent so mechanism answers are ordered explanations ending at the first successful outcome. This avoids hardcoded narrative structures while giving the AI enough causal/timeline context for richer answers.
- `reasoning-composer-3` tightens mechanism grounding after user QA found a false location bridge ("a casa sua" -> tea preparation). Reasoning Composer now prefers core mechanism starts such as `fills`/`immerses`/`transforms` before generic `takes`, keeps only near-prelude events before the operational mechanism, trims focused source excerpts after the first successful outcome, and explicitly tells AI Agent not to infer that the mechanism continues in a previous prelude location unless selected evidence says so.
- Flow Map runtime event/preview copying must preserve full payloads for large Knowledge/Reasoning outputs. Runtime events, Preview nodes, copy actions and replay paths should use the complete payload directly, with no `__truncatedForUi` wrapper or hidden short copy.
- Graph Query now emits `debug.evidenceTrace` for answer-quality QA. The trace lists each scored/linked chunk in document order with selection status, score, reasons, query-token matches, seed-label matches and event-chain term matches, making it clear when a relevant chunk is available but excluded by the current evidence ranking.
- Graph Query evidence selection now has a protected mechanism-evidence lane for process/healing/cause questions. It derives mechanism terms from the selected event chain, guarantees a larger minimum evidence window for these intents, promotes chunks with several mechanism-term matches before filling remaining slots by score, and records `debug.mechanismEvidenceTerms` / `debug.mechanismProtectedChunkIds` for QA.
- The mechanism-evidence lane now avoids short substring false positives and derives terms only from the operational mechanism event window, not from every selected event. Reasoning Composer also preserves `mechanism-protected` source excerpts in order, so protected Graph Query evidence reaches `primaryEvidenceText` instead of being replaced by only the highest-overlap final-scene chunks.
- Follow-up QA showed consequence chunks could still be protected through outcome words while setup chunks with short terms like `tè` were under-matched. Mechanism protection now classifies protected chunks as `setup`, `operation` or `outcome`, requires at least two operational matches for setup/operation or two outcome matches plus a success cue for outcome, allows short terms only as exact tokens, and exposes `operationalMatches`, `outcomeMatches` and `protectedKind` in the evidence trace.
- Graph Query evidence behavior is now configurable per node. The node exposes `Evidence mode` (`focused`, `balanced`, `full_ordered`, `debug_trace`), `Max evidence chunks`, `Include adjacent chunks`, `Preserve document order` and `Protected evidence`; `balanced` is the default, `full_ordered` preserves all scoped chunks for recall/QA, and the behavior is documented in `docs/ai/flow-map/runtime-graph.md`.
- Flow Map runtime config now has the first Payload Editor foundation for Manual JSON, Task and Graph Query nodes. Payload rows are stored as `metadata.config.payloadItems`, each row has independent `enabled` and `visible` controls, optional `icon`, `iconColor` and `description` tooltip metadata, supported value types (`string`, `int`, `float`, `boolean`, `note`, `select`, `json`), inline JSswift value controls, Add/Edit item dialogs for key/label/type/options/icon/icon color/description, enabled Manual JSON rows rebuild the emitted JSON payload, and visible Manual JSON/Task rows render as editable JSswift controls directly on the node card without an extra row frame.
- `subject-resolution-v2` starts the next event-quality layer: Knowledge Event Builder now keeps a short narrative participant memory from dictionary-backed proper nouns/roles and uses it to resolve plural/implicit action subjects only when at least two recent participants are supported by context. Explicit subjects can also retain a previous participant when a reliable object/indirect pronoun is present. This improves events such as plural preparation actions without hard-coding document names and keeps unresolved subjects unresolved when confidence is low.
- `reasoning-composer-4` fixes the first hybrid Composer QA payload: the plan was in `hybrid` mode but attempted `local_ollama` and failed before any LLM patch, so downstream LM Studio answered from a mostly rule-built plan. Knowledge Reasoning Composer now prefers the local LM Studio profile when no explicit Composer provider is set, source-intent plans filter supporting relations to source-like evidence, source excerpts become first-class required facts, and the runtime worker cache-buster was updated so the browser loads the new Knowledge runtime.
- `reasoning-composer-5` hardens the next hybrid Composer QA payload: LM Studio was reached correctly but Gemma returned non-JSON, leaving `ai.error=invalid-ai-json`. Composer now retries LLM planning as `full -> compact -> micro` with a stricter JSON-only prompt, accumulates token usage across attempts, and salvages non-JSON prose only as low-confidence `answerFocus` while keeping quotes/evidence validation unchanged.
- `reasoning-composer-6` is superseded by the no-filter rollback: Composer may accept LLM focus/quotes, but it no longer adds one-sentence boundaries or controls answer brevity.
- `reasoning-composer-7` is superseded by the no-filter rollback: source intent still improves evidence retrieval/order, but Composer must not rewrite final answer scope to speaker-only or block downstream detail.
- `reasoning-composer-8` fixes the downstream AI Agent consumption path: source Composer plans already produced the correct `answerFocus`, but AI Agent still expanded from the long evidence quote. AI Agent graph prompts now add a source-answer directive, treat source `answerFocus` as the complete answer when it matches evidence, and use evidence only for verification instead of summarization.
- `reasoning-composer-9` adds a deterministic AI Agent output guard for source-intent reasoning plans. If the plan contains a validated `Risposta breve: ...` answer focus, the emitted clean answer/text is normalized to that speaker identity even when the underlying LLM expands the quote content.
- `reasoning-composer-10` adjusts the source-answer guard from ultra-synthetic labels to natural short answers. Source intent now keeps a one-sentence answer shaped from the user question, for example "L'anziano dice a Juliette come guarire Liber.", instead of reducing the answer to only "l'anziano".
- `reasoning-composer-11` fixes a source guard duplication bug: when `Risposta breve` already contains a natural sentence with a source verb, AI Agent now emits it directly instead of appending the query predicate a second time.
- `reasoning-composer-12` restores user-prompt authority for detail requests. Source-intent guardrails now apply only to simple source questions; queries asking for details/explanation keep the focused source evidence and allow the LLM to include the relevant revealed details instead of being forced to a short answer.

Knowledge Graph Query baseline is complete:

- `Graph Query` is a first-class Knowledge node that reads persisted entities/relations and emits `knowledge.graph.context`.
- Graph context includes matched entities, neighboring relations, scoped evidence chunks and a compact prompt-ready context block.
- AI Agent runtime recognizes `knowledge.graph.context` and injects it into prompts/job metadata like RAG context.
- `Knowledge Graph Test` includes `Graph Query -> Preview -> AI Agent`, so agents can consume graph information, not just visualize snapshots.
- Graph Query now supports `preferLatestDocument` so sample flows keep working when the user replaces the uploaded document inside the same collection.
- AI Agent graph/RAG responses now emit a clean preview payload (`question`, `answer`, compact relations/entities/evidence or sources) while the AI debug job keeps the full prompt and raw context for inspection.
- Knowledge runtime now routes events by actual incoming dependencies when a node has visual links, preventing disconnected Graph Query/source nodes from continuing to consume same-channel events.
- AI Agent runtime now applies the same dependency-aware event routing, so linked agents do not consume unlinked Graph/RAG context events from the same global channel.
- Graph Query now treats `knowledge.graph.updated` as an index-refresh signal only; it no longer falls back to a saved `config.query` on graph updates without an explicit query payload.
- Graph Query requires a visual `knowledge.graph.updated` graph-source edge before it reads persisted graph data; without that edge it emits an empty graph context instead of querying the workspace store. Graph updates still do not trigger a query by themselves.
- Flow Map forced runtime reload now explicitly refreshes the background runtime worker, so deleting a link updates live Knowledge/AI subscriptions without requiring a browser refresh.

Flow Map library separation baseline is complete:

- global sidebar `Flow Map` now opens `libraryFlowmap.html` instead of a single workspace flow;
- `libraryFlowmap.html` lists local `.tlflow` Flow Maps from SQLite and opens `flowMap.html?workspaceId=...`;
- Flow Map library sidebar filters by searchable categories, while sorting is in the result toolbar;
- Flow Map library `Nuovo Flow Map` opens a JSswift creation dialog for title, description, category, color and version, then creates dedicated `tl_pages` and `tl_flows` records;
- Flow Map library cards include a confirmed delete action for the local flow, scoped runtime graph, channels, events, logs and connections;
- Flow Map library cards now use per-flow accent colors, a mini graph visual layer, metric pills and a card color picker persisted as UI metadata without changing flow `updatedAt`;
- Flow Map export from `flowMap.html` now downloads `.tlflow` bundles with embedded assets and runtime graph;
- Flow Map import accepts `.tlflow` while keeping `.tlworkspace` compatibility.
- Flow Map and asset library environments are separated: `library.html` filters out Flow Map page records, while `libraryFlowmap.html` lists only `.tlflow`/flowmap-marked records and imports legacy bundles as Flow Map when invoked there.
- Asset library cards now have a modern gradient/accent treatment, single-line left-aligned titles, UI-only color picker, no workspace Flow button and confirmed delete actions.
- Workspace editor owns Lens insertion: Flow Map no longer exposes Lens palette items, while `editorWorkspace.html` shows draggable/clickable Lens templates plus saved boxLens assets in separate collapsible left-panel sections with edit actions.
- Box editor dialog baseline: `boxEditorDialog.js` now provides a JSswift, iframe-free universal settings dialog for creating/editing boxLens and boxTracker assets from Workspace and Flow Map surfaces, with local `tl_widgets` persistence and draft runtime promotion support.
- Workspace editor layout now keeps workspace properties in a fixed drawer opened from the title edit button, removes the right grid column and keeps the mini navigator as a right-side fixed overlay with persisted minimize/expand state.
- Flow Map composition baseline adds `Flow In`, `Flow Out` and embedded `Flow Map` palette nodes; embedded Flow Maps can be inserted only when the target Flow Map exposes at least one Flow In/Out port, and workspace boxLens linking can now use composable Flow Maps as hidden sources alongside boxTrackers.
- Flow Map boundary ports are configurable: `Flow In` and `Flow Out` nodes expose an Add/Edit port dialog with typed ports (`string`, `int`, `float`, `object`, `array`, `bool`), render as dedicated gateway cards, place Flow In ports on the right and Flow Out ports on the left, and embedded/workspace Flow Map links inherit the configured port definitions.
- New Flow Maps created from `libraryFlowmap.html` automatically start with configured `Flow In` and `Flow Out` boundary nodes, positioned on opposite sides with default `flow.in` and `flow.out` object ports.
- Embedded Flow Map insertion now lists every available Flow Map in a selection dialog; insertion happens only from the explicit `Inserisci` action, which reloads the selected graph and blocks with an alert unless at least one `Flow In` or `Flow Out` node still exists.
- Embedded Flow Map nodes expose and automatically refresh the source graph interface: internal `Flow In` definitions render as typed inputs on the left, internal `Flow Out` definitions render as typed outputs on the right, generic `all` ports remain available, and alias deletion removes only the virtual node plus its local links.
- Embedded Flow Map aliases include a `View Flow Map` action that opens a read-only JSswift dialog with a fitted minimal graph canvas, compact name/type node cards and source-colored connection paths loaded live from the referenced Flow Map.
- Embedded Flow Map `View Flow Map` now shows configured IN/OUT ports in the read-only graph preview, and the Flow Map File menu includes `Export JPG` for a full graph image rendered from the same preview/export model.
- Universal box editor is now the primary editor path for boxLens and boxTracker creation/editing across Workspace, Flow Map, Library and Database Explorer.
- Legacy `editorBoxLens.html`/`editorBoxTracker.html` pages and their editor CSS/JS have been removed; the universal dialog is now the only boxLens/boxTracker editor surface.
- Tracker manual test execution is centralized in `core/runtime/tracker-test-runner.js` and consumed by the universal dialog.

Flow Map canvas interaction refinement is complete:

- canvas wheel now pans naturally vertically and horizontally, with Shift-wheel as horizontal fallback for wheel mice;
- Ctrl-wheel zooms around the pointer and persists the viewport;
- Ctrl-wheel updates the canvas topbar zoom percentage without waiting for a remount;
- Shift-drag on a runtime node moves that node plus all downstream child nodes while preserving relative distances;
- Alt/Option-click on the node delete quick action deletes immediately without opening the confirmation dialog.
- The node delete dialog includes `Force Delete All Children` when downstream nodes exist, deleting the node plus every recursive child node.
- The Flow Map canvas now shows a red center marker at graph center and the edge canvas draw area is wider to avoid clipping long cables.
- Runtime Minimap now fits graph bounds instead of zoom perspective, shows the visible viewport in graph coordinates, updates live during canvas pan/zoom, supports dragging the viewport box and can be minimized with persisted state.
- Runtime Minimap node markers now inherit each node tone color for clearer graph orientation.
- Runtime Minimap node markers now use rectangular 2px-radius shapes sized from each node's rendered height after first paint, including collapsed nodes.
- Preview node payload header now keeps action buttons visible with long channel names, and newly created Preview nodes ignore cached events older than their creation/clear timestamp.
- Sample Test nodes now normalize numeric flow positions into CSS percent values before rendering, preventing stacked nodes until the first drag repaint.
- Knowledge Sample Test now uses a wider margin-aware layout with larger row spacing so generated nodes do not overlap the rows below.

Runtime contract base is complete:

- official Workspace/Page -> Flow -> Runtime Nodes -> Runtime Dependencies -> Connections -> Channels -> Events/Flow Logs contract documented;
- `core/runtime/runtime-contract.js` added as shared contract/schema/mapping helper;
- Flow Map runtime config now normalizes manifest `settingsSchema` before rendering config fields;
- Flow Map link creation now captures explicit connection mapping metadata;
- connection mapping is mirrored from `tl_connections.mapping` into runtime dependency metadata and shown in the edge inspector.
- Processor, AI Agent, Orchestrator Agent, Action and Storage runtimes now execute supported mapping modes before node-specific processing;
- mapping execution logs applied transforms and warnings in flow logs;
- `custom-transform` is intentionally stored but not executed until sandboxing is implemented.
- Preview nodes now display mapped payload, original payload and mapping warnings separately;
- Edge inspector mapping details now show mapping status and copy actions for transform/mapped payload;
- Existing runtime links can reopen `Connection Mapping` from Edge Inspector and persist mapping edits back to connection/dependency metadata;
- Flow Map topbar includes a `Mapping Test` diagnostic that creates Manual JSON -> Preview with a BTC `json-map` transform and emits a mapped test payload.
- Flow Map topbar includes a `Storage Test` diagnostic that creates Manual JSON -> Save SQLite Record, emits through EventBus and verifies the mapped record persisted to SQLite.
- Storage node Inspector now shows the latest persisted SQLite record payload with copy/refresh actions.

Step 5 base is complete:

- Flow Agent memory now stores typed, confirmed workspace facts after successful Apply;
- rename and duplicate actions write node alias memory;
- endpoint config updates write endpoint-choice memory only after validation and Apply;
- Flow Agent planning enriches runtime context with confirmed memory aliases;
- AI command normalization receives confirmed memory context;
- DevTools AI memory table exposes memory kind for inspection without flooding normal chat replies.
- DevTools AI memory table now has Pin/Unpin and Forget controls for stored memory records;
- AI memory cleanup preserves pinned short-memory records;
- AI memory dialog now surfaces text, tags and provenance parsed from memory `meta`.
- DevTools AI memory dialog and Pin/Forget controls were manually verified in the AI tab.
- Flow Agent now stores explicit user preference memory from prompts such as "ricordati che...";
- Flow Prompt Chat exposes focused memory recall diagnostics for rename/duplicate aliases.
- Step 5 preference memory was manually verified in Flow Map AI Chat.
- Flow Map Mapping Test and Storage Test were browser-verified successfully.
- Runtime error diagnosis memory now stores structured provenance: record id, node, cause, suggestion and fix hint.
- Endpoint research helper now follows OpenAPI/Swagger specs, accepts explicit spec/doc URLs from the prompt as first-class sources and returns discovery provenance to the Flow Agent UI.
- Flow Chat routes prompts such as "cerca endpoint da https://...openapi.json per ..." to non-mutating endpoint research instead of blocked config updates.
- Flow Chat rejects OpenAPI/Swagger/docs/schema URLs as selectable runtime endpoint candidates; those URLs are allowed only as research sources.
- Flow Chat can parse an explicit public OpenAPI JSON source directly in the browser when the same-origin endpoint research helper is unavailable.
- Endpoint candidate cards show `researchSource`; AI fallback candidates are explicitly warned in the UI.

Previous work:

Step 4 base is complete:

- local compound prompts are split into ordered steps;
- each step is planned against a simulated context that includes previous step effects;
- AI-normalized `{ actions: [...] }` and `{ steps: [...] }` use the same simulated context;
- compound steps carry `stepId`, `dependsOn`, `compoundIndex` and `compoundPrompt`;
- duplicated nodes get a planned runtime id so later steps can reference them;
- the safe executor reloads runtime after each mutation before validating the next step.

Example now supported:

```txt
rinomina REST API in Weather API e collega Weather API a Preview
duplica REST API come REST 2 e collega REST 2 a Preview
```

API/backend Step 3 auth integration is complete and locally verified:

- `profile.html` now loads `js/tl-api-client.js`;
- Profile view reads current user from `TrackerLensApi.client.user()` on load;
- Profile view login calls `TrackerLensApi.client.login()` and then refreshes current user;
- Profile view registration calls `TrackerLensApi.client.register()` and uses the returned authenticated user payload;
- Profile view logout calls `TrackerLensApi.client.logout()` and clears the local session state;
- login/logout requests continue to use the existing API client CSRF/cookie flow;
- `TrackerLensApi` now refreshes CSRF after login/register and retries mutating requests once after a `419`;
- backend `.env` is configured for local SQLite and local Sanctum stateful domains;
- dashboard mock data was intentionally left unchanged.

Verification notes:

- Backend `GET /sanctum/csrf-cookie` returns `204` with CSRF/session cookies on `127.0.0.1:8000`.
- Local auth HTTP flow verified: CSRF `204`, register `201`, current user `200`, logout `204`, current user after logout `401`.
- Backend `php artisan test` passes: 9 tests, 49 assertions.
- `node --check js/profileView.js` passes.
- `node --check js/popup.js` passes.
- `node --check js/tl-api-client.js` passes.
- `profile.html` and `js/tl-api-client.js` are served successfully from `http://127.0.0.1:5173`.
- In-app Browser `iab` was unavailable in this session, so visual verification could not be completed there.

API/backend Step 4 dashboard integration is complete on the local Profile view:

- `profile.html` remains the account/API dashboard surface for this step;
- legacy `dashboard.js`/`options.html` dashboard-builder code was intentionally left untouched;
- Profile stats now use `TrackerLensApi.client.dashboard.summary()` after authenticated current-user load;
- Profile recent activity now uses `TrackerLensApi.client.dashboard.activity()`;
- Profile system status now uses `TrackerLensApi.client.dashboard.systemStatus()`;
- profile fallback data remains visible when the user is not authenticated or the backend is unavailable.

Verification notes:

- `node --check js/profileView.js` passes.
- Local authenticated dashboard HTTP flow verified against `127.0.0.1:8000`: register `201`, summary `200`, activity `200`, system-status `200`, logout `204`.

Knowledge Runtime document upload/import UX, Knowledge Graph quality/analytics and AI Agent RAG verification are complete and user-verified:

- Knowledge requirements audited after API/backend Step 4 was verified by the user;
- existing AI memory, runtime storage and provider registry were audited before adding new stores;
- Knowledge remains local-first, workspace-scoped and SQLite-based;
- `tl_knowledge_documents`, chunks, embeddings, entities, relations, queries, sources and metrics store constants were added;
- `core/runtime/knowledge-runtime.js` adds local stores, document creation, chunking, local deterministic vectors, cosine RAG search and EventBus integration;
- Flow Map now loads/syncs `TrackerLensKnowledgeRuntime` in page and worker contexts;
- Flow Map accepts `knowledge` as a first-class runtime type and exposes Knowledge palette nodes.
- Flow Map topbar now includes a `Sample Test` menu with Mapping Test, Storage Test and Knowledge Test; Knowledge Test creates a complete local sample graph: document source -> Document Store -> Chunk Processor -> Embedding Generator plus query source -> RAG Search -> Preview/AI Debugger, then emits sample document/query payloads.
- Flow Map Runtime Minimap is now always visible when nodes exist, supports click-to-jump and auto-centers on sample test result nodes after Mapping/Storage/Knowledge samples are created.
- Knowledge Test now cleans generated sample Knowledge records before rerun, and RAG Search deduplicates identical chunk text before building context.
- Knowledge chunking now reads `payload.document` from Document Store events and RAG Search filters serialized runtime/document envelopes from retrieval results.
- Knowledge Test now includes an `Embedding Preview`, explicit document/query emit channels and a hard check that a `tl_knowledge_embeddings` record is created before running RAG.
- Embedding Generator now resolves configured AI provider profiles for real embeddings (`ollama`, LM Studio/OpenAI-compatible) while keeping local hash embeddings as the offline fallback; RAG query vectors are generated with the same provider/model as the stored embeddings.
- Runtime node config save now parses JSON entered in the generic `Runtime Config` textarea and flattens accidental nested `config` JSON strings instead of persisting `metadata.config.config`.
- Knowledge node config dialogs now use Knowledge schema fields directly and no longer prepend the generic `Runtime Config` textarea.
- Knowledge Test now shows Embedding Generator -> RAG Search as an index-ready link; RAG Search ignores embedding events without a query and still runs retrieval only from `knowledge.search.query`.
- RAG Search now scopes retrieval to connected `knowledge.embedding.created` source nodes when present, using embedding `metadata.nodeId`; without an embedding link it falls back to workspace-scoped search.
- Local provider embeddings now use `api/ai-embedding-proxy.php` for localhost LM Studio/Ollama endpoints when served through PHP, avoiding browser CORS preflight failures while keeping remote providers direct.
- Knowledge Test now preserves edited `Knowledge Doc Source` and `Knowledge Query Source` JSON payloads on rerun instead of always emitting the Adam sample payload; manual-json Pulse payloads prefer `config.json` over stale `testPayload`.
- Knowledge sample indexing now uses a stable document id plus `collectionId=knowledge_sample_current`; chunking replaces old chunks/embeddings for that document so edited sample text does not mix with previous Adam sample chunks.
- AI Agent runtime now uses `api/ai-chat-proxy.php` for localhost LM Studio/Ollama chat endpoints when served through PHP, avoiding browser CORS preflight failures.
- Knowledge Test now connects RAG Context -> AI Answer node -> AI Answer Preview, with a prompt that answers from `payload.query` and `payload.context`.
- Knowledge sample AI Answer now disables automatic input history and memory context (`inputDataMode=off`, `memoryMode=none`) so unrelated workspace events such as old BTC tasks are not injected into the RAG answer prompt.
- Test run completion now forces canvas/topbar refresh and Knowledge runtime propagates `runId` to downstream events, preventing node play buttons from staying disabled after Knowledge/AI child execution completes.
- AI Agent runtime now treats `knowledge.rag.context` as first-class RAG input: it normalizes query/context/sources, injects a Knowledge RAG prompt block, exposes `ragContext` to templates and persists RAG metadata in AI jobs/results/events.
- Flow Map node inspector now shows an `AI RAG Debug` panel for AI Agent nodes, reading the latest `tl_ai_jobs` record and surfacing provider/model/fallback status, RAG query metadata, copied context and top RAG sources with scores.
- Knowledge Graph Step 6 base is implemented locally: `Entity Extractor` now extracts deterministic entities from chunks/manual text, persists `tl_knowledge_entities`, creates `co_occurs` relations in `tl_knowledge_relations`, emits `knowledge.entity.created` and `knowledge.relation.created`, and `Knowledge Graph` emits a `knowledge.graph.updated` snapshot with entity/relation counts and top entities.
- First manual Knowledge Graph test produced a valid snapshot; follow-up fixes now propagate `collectionId` from Entity Extractor to graph snapshots, classify single proper nouns such as `Adam` as `proper-noun`, suppress short duplicate entities such as `LM` when `LM Studio` exists, and generate clearer relation ids from normalized labels.
- Knowledge Graph inspector base is implemented: Knowledge nodes now show a `Knowledge Graph Debug` panel that reads local entity/relation/metric stores, shows counts, latest snapshot scope, top entities, recent relations, refresh and copy actions.
- Knowledge Graph visualizer dialog base is implemented and user-verified: Knowledge Graph nodes expose `View Graph` from the node body and inspector; the dialog includes search, type filter, relation filter, layout mode, node limit, grouped legend/index, selection details and a canvas graph view colored by entity type and sized by degree.
- Document Store upload UX is implemented: Document Store/Text Knowledge/Memory nodes expose `Upload Document` and document-count `Documents` actions from the node body plus `Knowledge Document Debug` in inspector; uploads accept `.txt`, `.md`, `.json` and `.csv`, emit a runtime EventBus document payload, and the documents dialog lists uploaded files with search, chunk counts, preview, copy and refresh actions.
- Knowledge document dialog now supports JSswift search/select controls and confirmed document deletion, removing the selected document plus derived chunks, embeddings, entities, relations, sources and graph metrics.
- Knowledge upload now uses a DOM-attached file input and FileReader progress state, with visible selected/reading/processing/complete/error progress bars on the node, inspector and documents dialog; the dialog refreshes its document list after a successful upload.
- Knowledge upload now persists the selected file directly through `TrackerLensKnowledgeRuntime.createDocument` before emitting `knowledge.document.created` downstream, so upload no longer depends on the Document Store event listener being active.
- Knowledge document panels now show a compact preview plus text length metadata; longer documents expose a JSswift `View Full Document` dialog for the complete stored text, and empty upload-progress placeholders no longer render as `null`.
- Uploaded/replayed documents now keep their payload scope ahead of stale node config: document title, source type, mime type, collection id and document id from uploads propagate through chunks, embeddings, entities and graph snapshots so old sample scopes such as Adam/`knowledge_test` do not override the current file.
- Entity extraction now filters common function words across Spanish/Italian/English/French and normalizes accents in generated ids, reducing noisy graph entities such as `Con`, `Para`, `Sin`, `Los`, `Muchas`, `Ahora` and preserving cleaner ids like `busqueda`.
- Entity Extractor now supports `extractionMode` (`strict`, `balanced`, `wide`) and custom `stopWords`; default `strict` keeps seeds, URLs/emails/symbols/technology, multi-word entities and repeated single proper nouns, reducing narrative one-off words in user-facing graphs.
- Entity Extractor now ignores direct `knowledge.document.created` input by default and processes chunk payloads only; `allowDocumentInput` can be enabled explicitly for direct document extraction, preventing duplicate entity/relation runs when both document and chunk events are connected.
- Entity phrase cleanup now strips narrative stopword prefixes/suffixes before candidate persistence, so phrases like `Cuando Juliette`, `Pero Juliette`, `Aunque Juliette`, `Era Liber` collapse toward the meaningful entity token instead of becoming separate graph nodes.
- Flow Map and runtime worker script URLs now include a Knowledge entity-cleanup cache-buster so browser/worker caches do not keep running an older `knowledge-runtime.js` after extraction changes.
- Document Store Play now replays an existing stored document by emitting `knowledge.document.created` downstream instead of sending the text back into the Document Store input, preventing infinite duplicate document creation on every Play.
- Step 8 Document Store upload/import UX is closed as user-verified after manual testing of upload, document listing, delete, preview/full document, graph debug and canvas graph visualizer interactions.
- Step 9 Knowledge Graph quality and analytics is complete: the View Graph `Info` pane surfaces graph-level quality signals from the visible graph, including average degree, density, connected components, isolated entities, repeated relation pairs and clickable top hubs.
- Knowledge relation quality now deduplicates equivalent source/target/type relations across chunks at document scope, preserving `occurrenceCount` and `chunkIds` metadata so repeated evidence does not over-densify the visual graph.
- View Graph `Info` now reports repeated evidence from relation `occurrenceCount` and lists the strongest repeated relations, making the deduplicated graph easier to validate.
- Entity quality now canonicalizes conservative water-source aliases such as `fuente de agua mágica/cristalina` -> `fuente de agua` and `agua del río` -> `agua`, preserving original labels in entity metadata aliases and surfacing aliases in the View Graph selection pane.
- Knowledge Graph snapshots now include top-entity aliases so copied graph data can confirm which labels were canonicalized.
- Relation quality now includes deterministic narrative relation inference from local context windows, adding stronger relation types such as `helps`, `heals`, `confronts`, `uses`, `travels_to`, `transforms` and `reveals` before falling back to generic `appears_in`, `interacts_with` or `co_occurs`.
- Narrative relation inference now rejects self-edges after alias canonicalization and tightens `helps`, `reveals` and `transforms` to avoid obvious false positives such as object self-transforms or location-to-person help relations.
- Italian Knowledge Graph quality now filters common Italian pronouns, determiners, question words and numeric-only labels before entity persistence, reducing noisy nodes such as `Egli`, `Chi`, `Suo`, `Sua`, `Cosa`, `Chiedilo` and numeric fragments across any Italian document.
- Italian Knowledge Graph quality now also rejects common capitalized narrative/speech verbs before they become proper-noun entities, reducing nodes such as `Andiamo`, `Venne`, `Aiuto` and stripping prefixes such as `Sussurrò Ombra Due`.
- Italian semantic classification now maps common biblical/abstract Italian labels such as `Vita`, `Morte`, `Giustizia`, `Scrittura`, `Luce`, `Ombra` and `Parola` to `concept`, and object labels such as `Arca`/`Croce` to `object`, instead of treating them as proper nouns.
- Italian semantic classification no longer promotes all biblical concept/object terms through broad keyword extraction; it only re-types candidates already found by existing extraction, avoiding the entity count spike seen in the Italian graph export.
- Italian graph cleanup now filters residual pronoun-like labels such as `Tutti`/`Colui`, classifies `assoluzione` as a concept and canonicalizes numbered shadow variants such as `Ombra Due` back to `ombra`.
- Italian cleanup now filters residual connective labels such as `Poiché` that can appear as capitalized sentence starters in relation exports.
- Italian cleanup now filters residual incomplete title/adjective labels such as standalone `Sommo`.
- Italian relation quality now adds conservative context-driven relation types before `co_occurs`: `fulfills`, `foreshadows`, `establishes`, `teaches`, `represents` and `opposes`, using explicit local textual cues such as `adempie`, `prefigura`, `alleanza`, `insegna`, `rappresenta`, `sacrificio`, `peccato` and `morte`.
- Entity typing now uses local source/book/document/work cues to classify source-like entities as `source`; source/entity pairs map to `mentions`, reducing generic book/person `co_occurs`.
- Source/source pairs now map to `references`, reducing another class of generic `co_occurs` edges without hard-coding domain-specific source names.
- Italian cleanup now filters residual capitalized verb labels such as `Doveva`, `Viene`, `Vengono`, `Veniva` and `Venivano` before they become graph entities.
- Knowledge relation quality now normalizes symmetric edge orientation for generic relations such as `co_occurs`, preventing duplicate opposite-direction pairs such as `Abrahamo -> Dio` and `Dio -> Abrahamo`.
- Italian cleanup now filters residual helper/connector labels such as `Aveva` and strips sentence-start prefixes such as `Così/Cosí` before candidate labels are persisted.
- Italian cleanup now filters additional capitalized sentence-start/common-word labels such as `Eravamo`, `Siamo`, `Sono`, `Dopo`, `Qualcosa`, `Semplice`, `Vecchio`, `Via`, `Uscita`, `Ogni` and `Presto`.
- Quote extraction now rejects digit-heavy fragments and page/chapter artifacts such as `112 L’`, requiring at least two lexical words before a quote entity can enter the graph.
- Symbol extraction now rejects generic all-caps Italian words such as `ABITA`, `COMPIUTO`, `MEDIANTE` and `SANGUE`; only technical/acronym-like tokens such as `HIV`, `AIDS`, `API`, `RAG` or tokens with digit/separator markers are kept as symbols.
- Entity extraction cleanup now falls back to the document id found on input chunks when `payload.documentId` is absent, so stale entities from the same document are removed on regeneration instead of lingering in graph exports.
- Knowledge Graph snapshots now apply the same weak-entity and stopword filters as extraction, so stale low-quality entities already stored in SQLite are not counted or rendered even before a full extractor rerun.
- Knowledge Graph inspector/View Graph now applies the same UI-side weak-entity filter before rendering or `Copy Data`, so raw SQLite entities such as stale all-caps symbols do not leak back into graph exports.
- View Graph export now has two explicit actions: `Copy Graph` exports only connected entities/relations, while `Copy With Isolated` includes filtered isolated entities for full audit exports.
- View Graph canvas now opens zoomed out at 85% with no default selected node, keeps non-focused nodes more readable when a node is selected and clears focus by clicking the canvas background.
- View Graph canvas now disposes pending animation frames and pointer handlers before rerender/close, preventing repeated dialog opens from leaving active canvas work behind.
- Narrative relation inference now uses stricter local windows for `heals` and removes generic `camino` as a `travels_to` trigger, reducing false positives where nearby characters are not the actual healed target or where a path is only being mentioned.
- Narrative relation inference now also requires local person/object action windows for `uses`, reducing broad object-use edges caused by a person and object merely sharing the same chunk.
- Narrative relation inference now tightens `heals` again so cure mentions alone do not mark nearby advisors/observers as healed; the healed person needs local drink/speech/voice/miracle evidence.
- `heals` now requires local drink/take/voice evidence near the healed entity; generic `hablar`/miracle mentions were removed as direct triggers because they marked nearby advisors as healed.
- `heals` now requires local drink/take evidence near the healed entity; `voz`/voice was removed as a direct trigger because it also matched descriptive voice text for non-healed characters.
- `heals` now requires the full local pattern: drink/take evidence near the person, cure-object evidence near the object and a speech/voice/miracle outcome in the same context window.
- `heals` is evaluated before generic `uses` so cure scenes such as a character drinking magic tea are not swallowed by the broader object-use relation.
- `heals` now requires the healed entity to be the local subject of the drink/take verb, preventing nearby supporters such as Juliette from being marked as healed in the same scene.
- `heals` subject detection now checks the candidate person directly, allowing descriptive words between the name and drink/take verb so "Liber ... tomó/bebió" is captured without matching nearby words such as "esperanza".
- `heals` now uses a wider cure-scene context but only for candidate cure objects (`agua`/`té`), while `says` requires a proper-noun speaker plus quote to prevent quote-location false positives.
- `says` no longer falls back from any proper-noun + quote pair; quotes are linked only when the candidate speaker appears before the quote and near a speech/action cue.
- Speaker matching for `says` is capped to a tighter pre-quote window so nearby non-speakers in the same scene are not attached to the quote.
- Step 9 Knowledge Graph quality and analytics is closed after user verification with the Liber story export: `heals` resolves to `té de color rojo -> Liber`, `says` resolves only to `Liber -> QUIERO HABLAR`, and graph exports no longer include the earlier quote/location or nearby-supporter false positives.
- Step 5 AI Agent RAG verification is closed after user verification in the `AI RAG Debug` inspector: the Knowledge Sample AI Answer job shows a populated `RAG query`, non-empty `RAG context`, result metadata and scoped Knowledge Sample context.
- Sample Test now includes `Knowledge Graph Test`, a focused preset that creates `Manual JSON -> Document Store -> Chunk Processor -> Entity Extractor -> Knowledge Graph -> Preview`, emits a sample narrative document and waits for a valid `knowledge.graph.snapshot`; the graph node is triggered by the relation-created channel to avoid duplicate visual links/snapshots.
- Knowledge Graph Test cleanup now also removes stale runtime dependencies by preset id/source before recreating the graph pipeline, preventing overlapping duplicate edges on a single port after reruns.
- Knowledge Graph Test link records now match the existing Knowledge Test pattern: runtime dependencies use a `dep_*` id, carry `connectionId`, source/target types and the connection record stores `channel` plus endpoint/node refs so dependency repair does not create duplicate visual links.
- Event Bus broadcast delivery is now scoped by `workspaceId`, preventing two open Flow Map tabs from consuming each other's Knowledge Graph Test events through the shared BroadcastChannel.
- Flow Map refresh now guards against overlapping `loadRuntime` calls and avoids restarting an already-running runtime worker for the same workspace, reducing renderer pressure from the 15s auto-refresh loop.
- Flow Map startup supports `repair=knowledge-graph`, which removes stale Knowledge Graph sample nodes/edges/events/logs and oversized runtime records for the selected workspace before the first render; runtime event/log payloads are also capped for UI rendering.
- Flow Map startup also supports `repair=hard`, a workspace-scoped runtime graph reset for plugin workspaces with corrupt node/edge records that crash after first render.
- Added standalone `flowMapRepair.html`, a minimal SQLite repair page that does not load Flow Map and can clear records referencing a corrupted workspace, including pages/widgets/runtime/knowledge collections.
- Knowledge Graph View now falls back to the latest snapshot document in the node collection when the node config still points to an older/sample `documentId`, so uploaded-document graphs are visible without manually editing node config.
- Knowledge Graph Debug/View now surfaces `Configured document`, `Latest snapshot document`, `Viewing document`, and a document status flag so stale config vs latest snapshot mismatches are visible.
- Flow Map canvas now uses world-unit node coordinates instead of viewport-percent placement for newly generated nodes, with viewport-aware edge canvas drawing and per-node width persisted in `flowPosition.width` via a node resize handle.
- Flow Map world-canvas follow-up is user-verified: new Flow Maps render/delete `Flow In` and `Flow Out`, and Sample Test presets no longer overlap.
- Reasoning Composer attribution/LLM follow-up: `source` intent is preserved for "chi dice/spiega/rivela..." queries, source evidence is boosted and wider snippets are preserved, and `Knowledge Reasoning Composer` now supports `compositionMode` (`rules`, `llm`, `hybrid`) where the LLM can propose answer focus and selected quotes while runtime validation preserves local evidence. LLM-proposed boundaries are no longer accepted.
- Reasoning Composer answer ownership follow-up: removed the `source` answer guardrails that forced short speaker-only answers or post-processed AI output. TL now keeps `source` intent for chunk selection, ordering and evidence grounding, but passes the focused evidence to the downstream LLM without deciding final verbosity or wording; the user's prompt controls whether the answer is brief or detailed.
- Reasoning Composer evidence-pack follow-up: focused evidence is cleaned before reaching the answer LLM. Composer trims leading/trailing chunk fragments on sentence/dialogue boundaries, deduplicates overlapping evidence blocks and removes repeated `Focused source excerpt` joins. LLM-proposed boundaries are ignored entirely; TL still owns only evidence quality and grounding, while final answer shape remains provider-owned.
- Reasoning Composer evidence-pack guardrail rollback: the source-window semantic narrowing was removed because it began deciding which parts of a source scene the answer LLM may see. The active cleaner remains mechanical only: sentence/dialogue boundary cleanup, overlap deduplication and readable required-fact evidence, without source-specific answer narrowing.
- Knowledge answer ownership is now a hard architecture rule: TL cleans and organizes evidence, but does not semantically narrow the response. The downstream LLM owns wording, detail level, tone and selection of supplied details according to the user's prompt. Any rule that hides semantically relevant evidence or replaces provider wording is a guardrail regression.
- Graph Query source expansion follow-up: source/attribution queries now add generic multilingual communication terms (`tells`, `reveals`, `explains`, `secret`, `solution`, etc.) only to retrieval/scoring and debug traces. The original query is unchanged, no book-specific names are injected, snippets use matched labels plus generic expansion terms, and no semantic narrowing is applied after chunks are selected.
- Graph Query expansion mode follow-up: Graph Query now exposes `queryExpansionMode` (`rules`, `llm`, `hybrid`). `rules` uses the generic local expansion terms, `llm` asks the configured provider only for retrieval terms/intent hints, and `hybrid` combines both after local token validation. The LLM expander never answers, never rewrites the user query and never adds final-answer boundaries; its terms are used only for retrieval scoring and debug trace visibility.
- Reasoning Composer boundary rollback: `excludedContext` is no longer requested from the Composer LLM, merged into the plan, or emitted as `Boundaries` in the final reasoning context. QA showed even well-intentioned boundaries can overconstrain the answer model. TL now keeps only grounded evidence, document order and light grounding notes; the downstream LLM owns final selection/detail/wording from the supplied evidence.
- Graph Query / Reasoning Composer danger-intent follow-up: questions about dangers, risks, threats, attacks, injuries or obstacles now use a generic `danger` intent for retrieval scoring, event scoring and evidence trace diagnostics. This is not book-specific and does not constrain answer wording; it keeps danger/challenge evidence ahead of unrelated process/healing events so the downstream LLM receives cleaner material.
- Danger-intent variant fix: danger detection now matches inflected terms such as `pericoli`, `pericoloso`, `ostacoli`, `ferendola` and similar multilingual variants instead of only exact stems. This fixes QA runs where Graph Query still reported `queryIntent.danger=false` for “che pericoli affrontano...”.
- Knowledge recall fix: Entity Extractor now accepts explicit person-to-concept quality evidence such as “grazie alla disciplina maturata” / “dimostrava...” as `has_property`, and Graph Query keeps matched seed entities plus their source chunks visible even when they have zero graph degree. This is recall protection only: TL exposes grounded evidence instead of hiding isolated but relevant facts, while the downstream LLM still owns final wording and semantic emphasis.
- AI Graph Answer preset prompt now defaults to evidence-grounded answer rules: the agent must answer only from graph context, reasoning facts and source evidence, must not add unsupported quantities/order markers/details such as “second flower”, and silently verifies concrete details before returning only the final answer.
- Semantic Relation Enricher LLM-first alignment: `enrichmentMode=ai` now bypasses local semantic rule output entirely, while `hybrid` uses local rules only when the provider fails or returns no semantic relations. The default node mode is now `ai`; TL still validates candidate ids, allowed relation types and grounded chunk context, but narrative relation decisions such as `healed_by` are provider-owned so per-node prompts can handle temporal/causal nuance.
- Knowledge LLM-mode alignment: Dictionary Builder, Entity Extractor, Event Builder, Graph Query expansion, Reasoning Composer and Semantic Relation Enricher now default to `llm`/`ai` for new nodes. `llm`/`ai` output is provider-owned after local evidence/JSON validation, and `hybrid` uses local rules only as fallback when the provider fails or returns no usable output. Prompt defaults for these nodes were expanded to require exact evidence quotes, source-language labels and no unsupported causality/order/count inference.
- Hybrid quality fallback: Dictionary Builder, Entity Extractor and Event Builder now treat very sparse LLM output as unusable in `hybrid` mode instead of accepting a poor memory just because the provider returned valid JSON. `micro` dictionary output, too few accepted entities/relations, or too few accepted events trigger a rule fallback, and payload metadata reports the threshold plus fallback reason.
- Dictionary LLM repair: pure `dictionaryMode=llm` now runs a dictionary-specific JSON repair pass for invalid provider output and then a single-chunk LLM retry path before returning empty. This keeps LLM mode provider-owned while avoiding empty dictionaries when smaller local models return non-JSON on full/compact/micro prompts.
- LLM chunk coverage fix: Dictionary Builder, Entity Extractor and Event Builder now default their LLM chunk limit to the full chunk count, capped at 40, instead of 8/12. Dictionary and Entity Extractor no longer accept sparse global/micro output as final when it is below a document-sized threshold; they continue into per-chunk LLM retries so pure LLM mode can cover the whole document without local rule fallback.
- Knowledge Dictionary Builder LLM-first follow-up: Dictionary Builder now exposes `dictionaryMode` (`rules`, `llm`, `hybrid`). The LLM path proposes reusable lexical entries, aliases, type candidates, semantic hints and relation cues from supplied chunks, while TL accepts only entries grounded in local chunk text/quotes and falls back to rules if AI returns nothing or fails.
- Knowledge Dictionary Builder JSON robustness follow-up: the LLM dictionary path now retries `full -> compact -> micro`, uses stricter JSON-only prompts, requests JSON response format when supported and falls back if that parameter is rejected. Valid JSON with no locally grounded candidates also retries smaller prompts before returning the rules fallback.
- Knowledge Dictionary Builder type-merge follow-up: LLM dictionary candidates no longer override strong local type evidence. If local context identifies a term as a proper noun, location, object, concept, role, source or technology, that type remains primary and the LLM type is kept only as secondary metadata.
- Entity Extractor LLM-first follow-up: Entity Extractor now exposes `entityMode` (`rules`, `llm`, `hybrid`) separately from local `extractionMode` (`strict`, `balanced`, `wide`). The LLM path proposes entities and relations from supplied chunks/dictionary terms, but TL persists only candidates grounded in local chunk text/quotes and only accepts LLM relations whose source/target resolve to accepted chunk entities.
- Entity Extractor JSON robustness follow-up: invalid LLM JSON now triggers a repair pass that converts the model output into the expected JSON shape before local validation. The `micro` retry is simplified to important entities only with empty relations, improving the chance of usable grounded candidates while rules continue to supply relation fallback.
- Entity Extractor chunk retry follow-up: QA confirmed the LLM path now succeeds but often falls back to `promptMode: micro` with AI entities and no AI relations. Entity Extractor now keeps those entities as fallback, then tries small per-chunk LLM passes for explicit grounded relations and deduplicates repeated dictionary/AI entity records in the output array.
- Entity Extractor AI relation-type follow-up: QA confirmed chunk passes recover AI relations, but local source-type inference could still replace validated LLM relation labels with generic `co_occurs`. AI relation types now take priority after local source/target/evidence validation, and relation metadata records the proposed LLM type.
- Entity Extractor relation canonicalization follow-up: free-form LLM relation labels are now normalized to TL graph relation types after local validation (`revealed_secret_to` -> `reveals`, `requested_information_from` -> `asks_for`, etc.), while the raw LLM label is retained in `metadata.ai.rawRelationType`. This keeps Graph Query/Reasoning Composer ranking compatible without hiding LLM evidence.
- Entity Extractor AI normalization follow-up: schema placeholders such as `concept` are rejected as AI entities, Italian/free-form labels such as `contiene`, `receiving_care_from` and `instructed_to_bring` map to canonical TL relation types, and `guard/guarda` no longer falsely matches healing stems like `guarire`.
- Reasoning Composer danger cleanup follow-up: QA showed the final danger answer was good, but the plan still promoted healing/movement events into `Required event chain` because entity token overlap was enough for `danger`. Danger reasoning now requires danger cue text or explicit danger relation/event types, keeping required facts focused on threats/obstacles without narrowing final answer wording.
- Knowledge Event Builder LLM mode alignment: Event Builder now exposes `eventMode` (`rules`, `llm`, `hybrid`) while preserving legacy `extractionMode=ai` as an alias for `llm`. Event subject/participant fields are normalized to deduplicate repeated comma-joined actors before records reach Graph Query/Reasoning Composer.
- Knowledge Event Builder JSON robustness follow-up: Event Builder now requests JSON response format when supported, falls back if rejected and runs an event-specific JSON repair pass before reverting to rules. QA also showed `local_ollama` can still fail fetch; provider selection must point to the active LM Studio profile for LLM contribution.
- Knowledge Event Builder chunk retry follow-up: LM Studio/Gemma can still return invalid JSON for full/compact/micro event extraction. Event Builder now has a single-chunk retry path that asks for at most a small event set per chunk, aggregates valid repaired events, and only then falls back to rule events.
- Knowledge Event Builder text salvage follow-up: if a local model returns event-like text or broken JSON fragments, Event Builder now salvages only explicit event objects with allowed event types and local chunk quotes. This prevents losing useful LLM extraction while preserving the rule that TL validates evidence and does not invent or semantically narrow final answers.
- Knowledge Event Builder quote-quality follow-up: salvaged/AI event evidence now rejects very short quotes and parses double-quoted Italian text without treating apostrophes as string terminators. This prevents partial snippets like `Tuttavia, c` from entering event context.
- Knowledge Event Builder event-cue follow-up: AI/salvaged `finds` and `discovers` events now require an explicit find/discover cue in the evidence quote. Search/intention phrases such as “looking for” are not persisted as discovery events.
- Knowledge Event Builder quote-boundary follow-up: AI/salvaged event quotes now must align to clean word boundaries in the source chunk. Mid-word cuts such as `... persone riu` are rejected as evidence-boundary errors before event persistence.
- Reasoning Composer danger retrieval follow-up: generic danger/challenge scoring remains only a retrieval/order diagnostic for events, relations and evidence traces. It must not become an answer filter. LLM-selected quotes are accepted when they are mechanically grounded in the supplied evidence, without additional intent-specific quote rejection.
- Reasoning Composer no-filter follow-up: Composer no longer accepts LLM-generated response instructions. It can contribute an evidence focus label and grounded selected quotes only; the runtime adds one neutral grounding note that final wording, detail level and emphasis follow the user's prompt. Removed mechanism-specific answer-shape instructions such as ordered explanation boundaries and “do not append...” rules.
- Reasoning Composer evidence-order follow-up: mechanism/source evidence from Graph Query now stays ahead of LLM-selected quotes in `primaryEvidenceText`, and merge keeps more ordered source paragraphs before deduping. This fixes QA where Graph Query had the right `fiore/sorgente/tazza/tè/beve/grido` chunks but the Composer put a single selected quote first and pushed the better source scene out of the final prompt. This is evidence packaging only, not semantic filtering.
- Reasoning Composer source-block follow-up: protected source chunks now bypass leading-fragment trimming and stay as source blocks before paragraph dedupe. This preserves mid-sentence chunk starts that still contain important evidence, such as setup/ingredient lines before a dialogue quote, and prevents early weak paragraphs from consuming the evidence budget before the operation chunk.
- Graph Query protected-boundary follow-up: protected mechanism evidence now marks chunk-boundary risk in `debug.evidenceTrace` (`starts-with-fragment` / `ends-with-fragment`) and automatically includes the adjacent chunk only when a protected source chunk is physically cut at the start or end. When this source-continuity repair activates, the evidence pack is emitted in document order. This is not semantic filtering: it preserves nearby text so the answer LLM receives complete evidence.
- Reasoning Composer focused-source follow-up: all Graph Query selected source chunks now preserve useful leading fragments and block boundaries inside `primaryEvidenceText`, not only `mechanism-protected` chunks. This is a physical evidence-continuity fix so the downstream LLM receives the retrieved scene; TL still must not decide final wording, detail level or semantic answer scope.
- Reasoning Composer source-boundary cleanup: focused source chunks are now ordered by document position after ranking and mechanically mark cut starts/ends with `...` rather than leaving naked partial words. This only exposes physical chunk boundaries; it does not remove evidence or constrain the downstream answer.
- Dictionary LLM diagnostics follow-up: Dictionary Builder now reports LLM chunk coverage in the OUT payload (`inputChunkCount`, attempted/productive chunks, covered chunk ids) and flags preview truncation separately from stored dictionary size. LLM dictionary validation now reuses the local lexical stop/weak-token checks and demotes unsupported lowercase proper-noun proposals to `term`, preventing false core seeds such as conjugations while preserving provider-owned extraction.
- Entity Extractor hybrid accepted-output fallback: `hybrid` now decides whether the LLM was usable from accepted/persisted entities and relations after grounding/dedup validation, not only from raw provider proposal counts. If the provider proposes enough candidates but TL accepts too few, a second rules pass fills the graph and reports `fallbackReason=sparse-accepted-entity-output`.
- Semantic Relation Enricher hybrid sparse fallback: `hybrid` now also treats too few accepted AI semantic relations as sparse instead of blocking rules just because the provider returned one or two valid labels. Sparse AI semantic output is preserved and supplemented by rule/supplemental relations with debug metadata.
- Hybrid fast fallback: Dictionary Builder, Entity Extractor and Event Builder no longer run exhaustive per-chunk LLM retries in `hybrid` mode. `hybrid` tries the provider with bounded full/compact/micro prompts, then falls back to local rules when the accepted output is sparse; per-chunk LLM retries remain reserved for pure `llm` mode where no rule fallback is allowed.
- Knowledge LLM debug cleanup: temporary console diagnostics for Knowledge node input and LLM requests were disabled after loop debugging, so normal runs no longer print `[TL Knowledge LLM #n]` messages in the browser console.
- Direct healing semantic rule: supplemental `healed_by` relations now require direct patient/mechanism action evidence such as drinking, taking, receiving, preparing or using the cure in the same local context. General aftermath/property statements such as anyone having the same possibility or water later possessing healing power no longer create `person -healed_by-> water/source` relations.
- Mechanism evidence priority cleanup: Graph Query no longer injects sample-material terms into mechanism/healing retrieval. Protected evidence should come from the query, selected events, LLM cue nodes or visible customRules, not hidden book-specific defaults.
- LLM mechanism evidence cues: Graph Query can ask the configured LLM for document-grounded retrieval cues for process/cause/healing questions. The cue output is locally validated against supplied chunks/events/relations, used only for evidence ranking/debug, and never becomes final-answer wording or a semantic boundary.
- Reasoning Composer mechanism focus guard: mechanism questions no longer promote LLM-selected focus text or quotes that point to background revelation/travel/later consequences instead of concrete operational evidence. The source evidence remains available, but ungrounded focus is not shown to the final AI answer node as a privileged instruction.
- Grounded mechanism terms cleanup: Graph Query no longer treats story-specific mechanism objects as fixed global memory. Evidence ranking should use query terms, selected events, LLM cue output or visible customRules instead of hidden sample defaults.
- LLM-only mechanism mode: in pure `llm` mode, Graph Query no longer adds rule mechanism expansions. The LLM mechanism-cue call now retries with a JSON repair pass, validated cue terms can pull matching chunks into the evidence candidate pool, and TL only validates grounding instead of substituting fixed mechanism objects.
- LLM cue failure broad evidence: if the mechanism cue LLM still returns non-JSON, TL salvages grounded terms from the raw LLM text when possible. If no cue survives validation, Graph Query marks `mechanismCueFailed`, broadens evidence instead of narrowing it by generic score, and Reasoning Composer receives a mechanical beginning/middle/end evidence sample.
- Danger typo/relevance fix: Graph Query now treats the common Italian typo `ennemico` as danger/enemy intent and event ranking no longer promotes unrelated events only from confidence. This prevents questions like “che ennemico?” from sending tea/healing evidence to the answer LLM when the requested enemy/threat evidence should be retrieved instead.
- Runtime LLM loop guard: Flow Map keeps page runtimes as the single execution owner while a test run is active, so silent refreshes do not restart the background worker into the same run. Knowledge Runtime now deduplicates input executions by node/run/input event id, matching the existing AI Agent and Orchestrator guard and preventing repeated LM Studio POSTs for the same Dictionary/Event/Entity input.
- Agent Tools Test bounded Knowledge modes: the generated Agent Tools sample keeps Dictionary/Entity builders in bounded `hybrid`, while Event Builder is now generated as `eventMode=llm` for focused pure-LLM Event QA. Pure `llm` Knowledge nodes can legitimately run global and per-chunk attempts; `hybrid` remains bounded to avoid loop-like provider logs during broader debug.
- Agent Tools legacy Event mode alignment: old Agent Tools sample Event nodes that lack an explicit `eventMode` now default to `llm` instead of being silently bounded to `hybrid`, matching the Event Builder UI default and the current pure-LLM Event QA path.
- Hybrid one-call LLM policy: Knowledge Dictionary/Event/Entity and Reasoning Composer now run only one provider attempt in `hybrid` mode and skip LLM repair/retry chains before falling back to rules. Existing Agent Tools sample nodes are detected by metadata and treated as bounded hybrid even if they were generated before explicit mode fields were added.
- Knowledge mode contract: every Knowledge node exposing `rules` / `llm` / `hybrid` must keep the same semantics. `llm` is provider-owned after neutral JSON/evidence/schema validation and must not merge local rule/anchor decisions into the accepted output. `hybrid` is LLM-first merge mode: it keeps all accepted provider output, then TL completes/enriches with local rules/anchors to cover gaps and deduplicates by grounded evidence. `rules` never calls the provider and stays deterministic/local. Prompt/system instructions remain the user-facing place to steer LLM behavior.
- Knowledge hybrid merge implementation: Dictionary Builder and Event Builder now use `hybrid` as LLM-first enrichment, not AI-or-rules switching. Accepted AI entries/events are preserved even when sparse, and TL adds rule/anchor completions with debug fields for `hybridMerged`, `ruleCompletionCount` and anchor/source counts.
- Knowledge Event Builder hybrid chunk merge: hybrid Event Builder no longer stops after one global LLM attempt when Gemma returns zero accepted events. It continues into bounded per-chunk LLM extraction to collect provider-owned events, then merges those accepted events with local rule completions.
- Knowledge hybrid status naming: Dictionary/Event hybrid payloads now report `status=partial-ai-merged` when accepted AI output exists but local completions are added because the AI output is below the hybrid threshold. `fallback` is reserved for zero accepted AI contribution.
- Knowledge Dictionary Builder hybrid chunk merge: Dictionary hybrid no longer stops after a sparse global LLM response. Dictionary JSON completion now has a larger minimum token budget, sparse global candidates are kept, bounded chunk LLM passes run to recover more provider-selected terms, and TL merges those AI terms with rule/anchor completions.
- Knowledge completion budget fix: LM Studio Knowledge calls no longer cap completions at a hard `context * 0.12` ratio that ignored user `maxTokens` values such as 2048. Completion limits now respect explicit node/provider max completion settings or a configurable context ratio while still applying a context-aware cap.
- Entity Extractor hybrid JSON/merge alignment: Entity Extractor now follows the same robust Knowledge LLM contract as Dictionary/Event. Invalid provider JSON can be repaired in hybrid, sparse global output continues into bounded chunk LLM passes, accepted AI entities/relations are preserved, and local extraction completes gaps with `hybridMerged`/`partial-ai-merged` diagnostics.
- Agent Tools Entity LLM mode alignment: the generated Agent Tools sample and legacy Agent Tools Entity nodes now default to `entityMode=llm`, matching the UI default and allowing pure Entity Extractor LLM QA before testing hybrid merge behavior.
- Entity Extractor LLM purity follow-up: pure `entityMode=llm` no longer runs the accepted-output sparse fallback pass. Local `seed`, `proper-noun` and `keyword-*` completion remains reserved for `hybrid`/`rules`, so LLM QA payloads expose only provider candidates that survived local JSON/evidence/relation validation.
- Entity Extractor AI relation normalization follow-up: free-form provider relation labels such as `spoke_to`, `cerca_cura_per`, `is_found_at`, `hosts_object`, `transforms_into`, `immersed_in`, `emanates_feature`, `similar_to` and `recalls_words_from` now map into TL relation vocabulary before persistence, with container-style `contains` oriented from container to content.
- Entity Extractor AI relation normalization follow-up 2: raw labels such as `action_on` and `performed_action_with` now map to stable interaction/usage relations, `has_knowledge_of` maps to `references`, and `leads_to` matching is token-bounded so `knowledge` can no longer be misread as `led`.
- Entity Extractor LLM relation contract: the LLM prompt and repair prompt now include `allowedRelationTypes`, require exact relation names from that list, and reject out-of-vocabulary relation labels at schema validation instead of persisting free-form model names. This keeps `llm` provider-owned while avoiding local extraction rules.
- Entity Extractor UI prompt defaults: new Entity Extractor nodes now show the same allowed-relation LLM contract in the Prompt tab by default, so users do not need to manually paste the relationType instructions after reset/new-node creation.
- Entity Extractor hybrid loop fix: `entityMode=hybrid` no longer runs per-chunk LLM retry passes after a sparse global provider result. Hybrid now makes a bounded provider attempt and then completes through local merge, while per-chunk LLM retries remain reserved for pure `llm`.
- Knowledge Rules Editor UI cleanup: the `Edit Rules` dialog remains visible for Knowledge nodes in `rules`/`hybrid` mode, including Entity/Dictionary/Event, but unsupported executable code is still ignored. Nodes whose rules are controlled by structured fields now show declarative metadata and the real runtime field names instead of an empty placeholder JSON.
- Knowledge custom rules activation: Dictionary, Event and Entity runtime paths now consume safe declarative `customRules` in `rules`/`hybrid` mode. Dictionary supports custom typed terms and stop/block terms, Entity supports custom typed entity terms, seed terms, stop words and allowed entity types, and Event supports custom event cue patterns, negative patterns, object hints and blocked event terms.
- Knowledge Rules Editor default transparency: Entity/Dictionary/Event `Edit Rules` templates now expose the current built-in declarative term/event lists instead of empty arrays, so users can see why rule/hybrid output is produced and can extend or replace those defaults.
- Knowledge Event Builder prompt-budget fix: hybrid Event Builder payload QA showed `promptBudget is not defined`, so the AI attempt failed before reaching LM Studio and immediately fell back to `rule-event-builder`. Event AI now uses the shared Knowledge prompt budget before building prompts, preserving the `llm`/`hybrid` contract and preventing false rules-only hybrid output.
- Knowledge AI editor config persistence fix: Knowledge AI nodes using the shared AI editor now read custom tab fields such as `eventMode` from the opened dialog as well as the form on save. This prevents the Event Builder UI from showing `llm` while the runtime still receives stale `extractionMode=hybrid`.
- Knowledge Event Builder LLM zero-accepted retry: pure `eventMode=llm` no longer stops after a provider response whose proposed/salvaged events all fail neutral evidence/type validation. That attempt is treated as retryable and the runtime continues into compact/micro/chunk LLM prompts without using rule fallback.
- Knowledge Event Builder LLM accumulation fix: pure `eventMode=llm` no longer returns immediately after the first valid global/salvaged event. It accumulates valid provider events across full/compact/micro and per-chunk prompts up to `maxEvents`, so one early `cannot_speak` event cannot prevent later document events from being extracted.
- LM Studio context budget: Knowledge LLM calls now apply a conservative local-provider context budget for LM Studio/Gemma-style models, defaulting to a 4096-token context unless configured otherwise. Dictionary/Event/Entity prompts cap chunk count, chunk chars, repair input and completion tokens so TL does not send prompt + generation budgets that exceed the local model context.
- Agent tool hidden-expansion rollback: connected read-only tools no longer inject danger/enemy sample terms before searching chunks, events, dictionary and graph facts. The LLM planner can still choose connected tools, but retrieval terms must come from the question, connected node evidence, LLM planning or visible configuration.
- AI Agent LLM planner: AI Agent now asks the configured LLM for a short JSON read-only tool plan before executing connected node tools. TL validates the planned node/tool pairs against the connected manifest, executes only read tools on reachable nodes, records planner intent/verification in the tool context, and falls back to the existing heuristic collector if the plan is empty or invalid.
- Knowledge graph clear controls: Entity Extractor now exposes its existing `replaceExisting` cleanup in node settings and a manual `Clear Entities` card button, Semantic Relation Enricher adds matching `replaceExisting` behavior plus `Clear Semantic`, and Knowledge Graph Builder Agent surfaces `replaceExisting` plus `Clear Builder`. Semantic cleanup excludes Builder Agent graph records.
- Knowledge AI chunk prompt budgets: Dictionary Builder, Entity Extractor, Knowledge Graph Builder Agent and Mechanism Cue Agent now expose `maxChunkTokens` instead of `maxChunkChars` in node settings and trim LLM prompt chunk text by estimated tokens. Existing saved `maxChunkChars` configs remain a legacy fallback converted to token estimates.
- Knowledge Graph Builder JSON recovery: Graph Builder Agent now requests JSON via the shared OpenAI-compatible JSON response helper when available, falls back if a provider rejects that format, and runs a repair prompt when the model response is not parseable JSON before moving to smaller prompt modes.
- Knowledge LLM Runtime visibility: Knowledge LLM nodes now persist runtime jobs into the shared AI runtime job store, replacing the old no-op `knowledgeLlmDebug` path with per-run input, prompt attempt, validation/result, fallback/error and emit steps. Prompt attempts flush while provider calls are still running and emit `knowledge.runtime.step` UI events, so `View Runtime` can update before LLM Studio finishes. Flow Map node cards expose `View Runtime` for Entity Extractor, Dictionary Builder, Event Builder, Semantic Relation Enricher, Graph Builder Agent, Mechanism Cue Agent, Graph Query and Reasoning Composer.
- Knowledge LLM Runtime follow-up: prompt attempt steps now remain `working` while the provider call is in flight and switch to complete only after the node finishes. Empty numeric AI config values now fall back correctly instead of becoming `0`, and explicit Knowledge node `maxTokens` values can exceed the old internal 1800 cap while still respecting the local context budget. Provider default completion caps/ratios no longer override a node-level `maxTokens`.
- Knowledge context-size guard: Knowledge LLM context-window config now ignores tiny numeric values below 1024, because a stray `context=6` was being treated as the model context window and forced LM Studio completion limits down to the 640 minimum despite `maxTokens=2048`.
- Knowledge max token authority: explicit node-level `maxTokens` now bypasses TL local completion caps entirely and is sent as the provider request value. Context/window values remain diagnostic/default-budget information, not a hard limit over the user's configured completion size.
- Entity Extractor runtime trace detail: `Full Runtime` now exports the complete job record, steps, prompt attempts, debug entries and result instead of only the input trace. Entity Extractor LLM attempts update their runtime step after the provider response with HTTP status, response length, JSON parse/repair status, proposed counts and accepted entity/relation counts, making full/compact/micro/chunk retry behavior inspectable.
- Entity Extractor LLM retry threshold fix: global LLM attempts now use chunk-proportional usability thresholds instead of requiring at least 12 entities and 8 relations for small documents. A 2-chunk document can now stop after a useful full/compact result instead of always falling through to micro plus per-chunk retries. Runtime request debug now includes `sentChunks` text previews and `promptTailPreview` so it is visible that chunk text, not only chunk ids, is sent to the provider.
- Runtime trace no-truncation follow-up: Knowledge/Agent Runtime trace payloads now preserve full prompt text, full sent chunk text and full step payload JSON in the runtime dialogs. The previous 1000/1600/5000-character debug/display truncations were removed from this runtime trace path.
- No hardcoded TL limits policy: Knowledge LLM/runtime paths must not silently cap user `maxTokens`, chunks, prompt text, repair text, graph context, evidence quotes, reasoning context, debug traces, previews, exports or persisted runtime data. Limits are valid only when explicitly configured by the user/node and must remain visible in Runtime.
- No-limit audit pass 1: AI Agent runtime no longer truncates RAG sources, graph context, reasoning facts/evidence, tool observations, continuation prompts, LM Studio error bodies or saved response summaries, and no longer caps final `maxTokens` at 32000, planner calls at 8, planner tokens at 700 or input history at 50 unless the user/node config explicitly sets a limit. Global Settings now uses a free numeric `ai.maxTokens` input instead of an 8192-capped range control.
- No-limit audit pass 2: Knowledge Dictionary/Event/Graph Builder/Semantic/Mechanism debug payloads now return full prompt/runtime-visible arrays unless a node config explicitly sets a domain limit. Knowledge completion debug no longer exposes misleading `localLimit/context` cap fields. AI Runtime Center provider/job/log/memory/prompt panels render all filtered records. Flow Map runtime state, Workspace payload previews, Action runtime HTTP bodies, connected read tools, event-log retention and sandbox numeric policies were aligned so TL does not silently truncate, cap, prune or partially export inspection data by default.
- Entity Extractor LLM relation coverage fix: pure `entityMode=llm` no longer returns immediately after the first globally usable response; it keeps the useful global result and still runs provider-owned chunk passes to improve relation coverage. Accepted LLM relation endpoints are now materialized as AI entity candidates when the model omitted them from `entities`, and final relation linking resolves canonicalized labels/aliases instead of requiring exact string equality. Runtime output reports accepted vs dropped LLM relation candidates so sparse graphs can be diagnosed from View Runtime.
- Entity Extractor zero-relation config fix: a user runtime trace showed prompts containing `"maxRelations":0`, causing Gemma/TL validation to accept zero relations even when the provider proposed 5-8 relation candidates. `maxRelations=0` and `maxRelationsPerChunk=0` now mean no explicit relation cap, matching TL's no-hidden-limits policy; only positive values limit relations.
- Entity Extractor relation diagnostics follow-up: request runtime payloads now include the raw provider response text and parsed JSON for Entity Extractor attempts. Final output exposes relation materialization counters and samples for created, duplicate, missing endpoint, same endpoint, cap skipped and dictionary-filtered relation candidates, so sparse LLM graph output can be traced without DevTools.
- Entity Extractor truncated JSON salvage: Gemma can emit useful but unterminated Entity Extractor JSON, causing full/compact attempts to be discarded and the run to fall back to `micro`, which intentionally has no relations. Entity Extractor now salvages complete JSON objects already present inside `entities` and `relations` arrays from invalid provider output, then runs the normal evidence/relation validation. Runtime marks these attempts with `salvaged: true`.
- World Generator Agent foundation: Flow Map now has a `World Generator Agent` Knowledge LLM node that accepts `task`, `raw` or `world.generation.request`, calls the configured provider, emits `world.record`, and leaves persistence to the explicit `World Database` node. Runtime debug records full worldbuilding prompts/responses, provider usage and generated record counts, and the Sample Test menu includes `World Generator Agent Test` for `Task -> LLM -> World Database -> World Graph View`.
- World Database generated-record root fix: records-only `world.record` payloads now synthesize a `world` record from `worldId`/`worldName` before persistence, so World Graph View has a root node and global items are no longer isolated. Parent normalization now reads `data.kingdomId` / `data.packId`, fixing generated records such as classes whose parent reference is nested inside `data`.
- World Database generated payload dedup: LLM world payloads can include both a `world` object with arrays and a parallel `records` array for the same IDs. World Database now deduplicates normalized world records before saving/emitting so Runtime/World Graph output counts match the actual graph instead of showing duplicate saved-result rows.
- World Database root record cleanup: when generated payloads include a full `world` object, the root `world` record no longer stores child arrays (`kingdoms`, `packs`, `classes`, names, story blocks, stories) inside `data`; those remain separate records. This keeps World Graph record detail readable and avoids duplicating the full world under the root node.
- World Generator count cleanup: runtime `recordCount` for generated world payloads now deduplicates `world.*` arrays and parallel `records` before reporting counts, so a valid 20-record generated world no longer appears as 39 records in the generator output.
- World Generator JSON repair: provider output that is otherwise usable but misses the closing `world` object brace before a valid `records` array is now repaired deterministically, with `repairedJson`/`repaired-invalid-json` diagnostics in Runtime instead of failing the node. `create_world` no longer injects existing stored world records into the prompt unless explicitly requested, preventing local models from mixing old world IDs with a new generated world.
- World Graph semantic projection: `World Graph View` records now project to more specific relation types instead of generic `belongs_to` everywhere. Kingdoms use `part_of_world`, packs use `belongs_to_kingdom`, classes use `available_in`, and stories use `set_in` / `set_in_world` plus `uses_block`. Global catalog records such as names, personalities and unreferenced story blocks remain saved/inspectable but no longer create artificial visual edges to the world.
- World Graph View UX follow-up: isolated worldbuilding records are hidden from the graph canvas by default to keep hierarchy/usage links readable, with an explicit `Show isolated` toggle and the full JSON/export still available for complete inspection.
- World Graph layout follow-up: force layout now derives visual parent/child levels from relation semantics and assigns each parent a vertical lane, so worlds/regni/branchi/classi/storie/story-blocks progress left-to-right with children placed forward near their parent instead of behind it or mixed with another branch.
- World Graph aside follow-up: selecting a worldbuilding node now shows graph links plus scoped catalog sections such as kingdoms, stories, name/personality pools, global classes, packs/classes or story blocks. Isolated catalog records stay off the canvas by default but remain browseable/clickable in the aside with full JSON inspection.
- World Graph aside scroll follow-up: the right aside now has its own viewport-bounded vertical overflow so long catalog/JSON sections scroll inside the panel without pushing the graph canvas.
- World Graph selected-record follow-up: the aside now renders a readable Details section for the selected record before raw JSON, so isolated catalog records show their type/id/parent/world and key data fields immediately when clicked.
- Worldbuilding character foundation: `worldbuilding/v1` now accepts `character`/`personaggio` records. World Generator can run `generate_characters`, World Database parses/validates characters, and World Graph projects `member_of`, `has_class`, `uses_name` and `has_personality` links so name/personality/class catalogs can become part of a living world graph.
- World Generator context overflow follow-up: `includeExistingRecords` now sends a compact existing-record reference context to the LLM prompt instead of full persisted world records, preserving all DB/runtime data while avoiding provider context overflow during `generate_characters`.
- World Generator worldId safety follow-up: non-create generation modes infer `worldId` only when existing context has exactly one world; otherwise they fail with a clear `world-id-required` error instead of silently creating `world_worldbuilding`. Character generation also instructs the LLM to use only existing pack/class/name/personality IDs.
- World Generator Task JSON follow-up: when a Task Node sends a JSON object as `objective`/`task` text, World Generator now parses and merges it before reading `worldId`, `generationMode`, counts and `includeExistingRecords`.

## Next Logical Step

Knowledge Dictionary Runtime is complete for the document-scoped product path. User verification covers Italian, Spanish, English, French and German; the Dictionary has evidence-grounded terms, scope/cleanup, ranking, Debug/View/export and consumer integration.

Current active work: Custom Node Packages. Phase 1 uses an importable `.tl-node.zip` artifact; Electron Main/TL Core validates and copies it into app data, SQLite catalogs its hash/provenance/permissions/trust, and runtime JavaScript stays blocked. Explicit cross-document/collection Dictionary promotion remains an optional, separately scoped future capability.

- Custom Node Package manifest-only Phase 1 is implemented: Core parses and validates `.tl-node.zip` archives, rejects unsafe paths/encryption/unsupported compression, stores the exact SHA-256 artifact in app data and writes opaque `tl_packages` metadata. Flow Map’s `Custom Nodes` dialog reviews manifest/permissions/trust/hash before explicit import and lists installed packages. Imported packages appear under `Custom Packages` in the node palette and can be placed on a Flow Map with their declared ports and package reference; their node card is visibly disabled and no live test or runtime code execution is possible. The renderer never sees a filesystem path and `runtime.js` is not loaded. Core also records a non-executing static runtime audit (dynamic code/process/module loading/network/filesystem/browser storage and undeclared permission mismatches) for transparent import review; it is not a safety verdict and does not unblock execution. Next: define the sandboxed runtime capability as a separate high-risk phase.
- Custom Node sandbox protocol foundation: `tl-custom-node-sandbox/v1` now defines the opaque Core-to-runner request and the only allowed runner messages (`ready`, declared-port `emit`, `log`, brokered `tool.call`, `result`). Core intersects declared and explicit user-granted permissions; it validates every output port and tool capability, while a manifest-only package remains non-executable. The actual runner is still pending and must use a disposable Electron sandboxed renderer with no Node/Electron/preload/package-path access and a Core-brokered channel; `vm`, child processes and utility processes are not accepted as isolation for untrusted JavaScript.
- Custom Node permission-consent foundation: declared manifest permissions and Core-owned granted permissions are now distinct. A new package starts with no grants; a confirmed consent must bind the exact package id/version/archive hash and Core intersects it with the declaration, so consent cannot expand authority. Flow Map exposes the consent state, but registration never enables the currently manifest-only runtime.
- Custom Node sandbox broker foundation: Core now owns the active-run state machine for `tl-custom-node-sandbox/v1`. It accepts only catalog-validated requests, revalidates each untrusted runner message against granted permissions and declared output ports, records an inspectable event trace and closes completed runs. The Electron sandbox window is still not connected, so no package code can yet run.
- Custom Node sandbox terminal-result contract: `result` accepts only explicit `success` or `failed` outcomes. The Core broker now offers one completion wait per execution and resolves it only from that validated terminal event (or its own runner failure), so future Flow Map channel delivery cannot treat an ambiguous/unfinished runner as complete.
- Custom Node Flow Map bridge: behind the sandbox feature flag, the narrow renderer bridge passes only package identity, node inputs/config/context and a bounded timeout to Core; Core returns only validated terminal diagnostics plus `emit`/`log` trace records, never source or a path. A sandboxed Custom Node is handled by Processor Runtime and re-emits each broker-validated declared port onto the Flow Map bus with execution provenance. Imported packages remain `manifest-only`, so no user package can reach this path until an explicit activation lifecycle exists.
- Custom Node sandbox activation gate: implemented. An imported package remains `manifest-only` until its declared permissions have been separately granted and the user confirms the CMS activation warning. Core then binds `sandbox-ready` / `runtimeExecution=sandboxed` to the exact package id, version and archive SHA-256; a changed archive/version must be reviewed and activated again. The palette reflects that state, while the Core feature flag remains authoritative.
- Custom Node Electron end-to-end gate: verified by `npm run test:custom-node-sandbox`. A local fixture runs inside the real disposable Electron sandbox, sends a declared-port `emit` and a log through the restricted preload/Main route, and the Core broker receives exactly those validated records. The fixture uses no package archive, filesystem, network or granted capability.
- Custom Node tool broker protocol: the sandbox now exposes only `tools.ai.complete`, `tools.memory.read/write` and `tools.runtimeGraph.read/preflight` wrappers. Each invokes a sender-bound Main route with a required unique `callId`; Core revalidates permission and arguments, records the request, and dispatches only to a registered Core handler. No handler is registered by default, so every otherwise-authorized tool fails explicitly rather than gaining implicit access.
- Custom Node read-only graph dispatcher: Core now registers `runtimeGraph.read` only when the sandbox feature flag is enabled. It reads the current execution workspace's runtime nodes/dependencies/channels through the allow-listed persistence API and returns a complete provenance-bearing snapshot without a path, DB handle or write ability. `runtimeGraph.preflight` returns a non-executable proposal only. The flag defaults to disabled, so this remains unavailable in normal operation.
- Custom Node safe-executor preflight: `runtimeGraph.preflight` now accepts only registered TL mutation tool names and returns a non-executable, workspace-bound proposal. `deleteNode` additionally lists affected dependency records from the Core store; no action is applied. A trusted TL UI/safe executor must still revalidate current state, capture Time Travel and obtain confirmation before any write.
- Custom Node activation state sync: placed Flow Map nodes now reconcile their exact package id/version/hash against a refreshed Core catalog, including on page reopen. A node created during manifest-only import moves out of its persisted `runtimeBlocked` state only after that exact package is explicitly activated. `npm run dev` enables the experimental sandbox.
- Custom Node user end-to-end verification: completed. An explicitly activated `Sample Text Inspector` package ran in a Flow Map Live Test and emitted its declared `diagnostic` output with `mode: runtimeResult`, a Live Test `runId` and node provenance. This confirms the full catalog activation → sandbox → Flow Map output path. Execution still requires exact-package activation and persisted permission consent.
- Custom Node Electron runner adapter: QA closed. A feature-gated (`TL_ENABLE_CUSTOM_NODE_SANDBOX=1`) ephemeral window uses a dedicated session, Chromium sandbox, context isolation, Node integration disabled, denied navigation/window-opening, denied browser permissions and denied HTTP(S)/WebSocket traffic. Its Main-process message route binds every runner message to the owning `webContents`, rather than trusting a package-supplied execution id, before the Core broker validates it. Core-only source loading requires a unique catalog reference, explicit `sandboxed` runtime state, archive hash/manifest revalidation and a bounded `runtime.js`; it never reaches the renderer. The Flow Map/Runtime Manager path is covered by the real Electron sandbox test and the user-verified activated-package Live Test; unactivated packages remain blocked.

## Required Updates When Work Changes

- Electron + Python migration: the Phase 5 managed Python POC now includes a `Python Test` Flow Map processor behind `TL_ENABLE_PYTHON_POC=1`. `Text Input -> Python Test -> Preview` uses a persistent standard-library worker and existing Event Bus output/error/status channels; existing JS nodes retain their ownership while persistence stays in TL Core SQLite.
- Electron + Python migration: Phase 6/7 base is complete. `tl_python_sdk.py` exposes explicit handler registration plus safe execution context/log/progress/cancellation, and `RuntimeManager.resolveCapability` maps `text.transform` to the feature-gated Python runtime without exposing language choice to callers.
- Update this file.
- Update `docs/ai/task-registry.md`.
- If architecture changes, update `docs/ai/decisions.md`.

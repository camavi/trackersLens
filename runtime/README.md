# Runtime

This folder is reserved for runtime-level notes, fixtures and future worker/runtime entry points.

Runtime implementation code should start in `core/runtime/` unless it is a page adapter.

Target runtime responsibilities:

- event bus
- channel registry
- dependency manager
- runtime graph
- event logs
- runtime inspector adapters

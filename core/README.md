# Core

This folder is reserved for reusable application logic that is not tied to a single page.

Planned first modules:

- `core/runtime/dependency-manager.js`
- `core/runtime/channel-registry.js`
- `core/runtime/runtime-graph-store.js`
- `core/runtime/event-log-store.js`

Existing page-specific code remains in `js/` until each runtime module is introduced safely.

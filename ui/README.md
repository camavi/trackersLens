# UI

This folder is reserved for shared UI composition patterns that are broader than a single page.

Trackers Lens should use CMSwift components for dialogs, toolbars, grids, cards, buttons, inputs, selects and standard panels.

Custom CSS/DOM should stay limited to:

- graph canvas
- runtime visual surfaces
- page-specific premium theme details
- interactions that CMSwift does not provide
